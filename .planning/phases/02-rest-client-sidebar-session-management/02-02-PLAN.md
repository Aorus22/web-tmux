---
phase: 02-rest-client-sidebar-session-management
plan: "02"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - desktop-gpui/crates/webtmux/Cargo.toml
  - desktop-gpui/crates/webtmux/src/app_state.rs
  - desktop-gpui/crates/webtmux/src/views/mod.rs
  - desktop-gpui/crates/webtmux/src/views/create_session_dialog.rs
  - desktop-gpui/crates/webtmux/src/views/session_states.rs
  - desktop-gpui/crates/webtmux/src/views/sidebar.rs
  - desktop-gpui/crates/webtmux/tests/state_view_test.rs
autonomous: true
requirements:
  - SESS-03
  - STATE-02

estimate:
  tokens: 60000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "User can open Create Session dialog (DLG1) from sidebar Plus button, EmptyState CTA, or SelectSessionView"
    - "Dialog requires name, validates client-side and server-side, offers optional cwd with native folder picker (App::prompt_for_paths), and optional initial command per D-01"
    - "Double-submit: the 2nd POST is a 409 that maps to the inline error line; no double session creation per D-01"
    - "Submitting a valid session creates it via POST /api/sessions, closes dialog, refreshes tree, and selects the new session"
    - "If backend returns error (400 invalid / 409 duplicate), an inline error banner displays the message in destructive styling without closing the dialog"
    - "EmptyState renders centered hero with TerminalSquare icon, 'No tmux sessions', and 'Create Session' primary button when tree.sessions is empty (STATE-02)"
    - "ErrorState renders AlertTriangle icon, 'Tmux is not installed', and 'Retry' button when tmux is missing or tree fetch fails (STATE-02)"
    - "SelectSessionView renders SquareTerminal hero, 'No session open', and 2-column session card grid with '{n}w' window badges when sessions exist but none is active (STATE-02)"
    - { statement: "Cancelling the native directory dialog leaves existing cwd untouched; selecting path populates input cleanly", verification: "backstop" }
  prohibitions:
    - "No mutation of tmux state from the GUI outside the Create-Session POST (no kill/rename UI this phase)"
    - "No silent dropping of backend error bodies (the error object must surface)"
    - "No polling without a generation guard"
  artifacts:
    - path: "desktop-gpui/crates/webtmux/src/views/create_session_dialog.rs"
      provides: "DLG1 Create Session dialog implementation using gpui-component Dialog, InputState, and App::prompt_for_paths"
    - path: "desktop-gpui/crates/webtmux/src/views/session_states.rs"
      provides: "ST1 EmptyState, ErrorState, and SelectSessionView workspace views"
    - path: "desktop-gpui/crates/webtmux/tests/state_view_test.rs"
      provides: "Unit tests verifying state routing between EmptyState, ErrorState, and SelectSessionView"
  key_links:
    - from: "desktop-gpui/crates/webtmux/src/views/create_session_dialog.rs"
      to: "desktop-gpui/crates/backend-client/src/rest.rs"
      via: "Dialog submit handler invokes RestClient::create_session and triggers AppState tree refresh"
      pattern: "create_session"
    - from: "desktop-gpui/crates/webtmux/src/views/session_states.rs"
      to: "desktop-gpui/crates/webtmux/src/app_state.rs"
      via: "Session state pages route based on AppState.tree and AppState.active_session"
      pattern: "render_workspace_body"
---

<objective>
Complete Phase 2 by implementing the interactive `CreateSessionDialog` (DLG1) with `gpui-component` Dialog/Input and native directory browsing (`App::prompt_for_paths`), and building the three central workspace state views (ST1: `EmptyState`, `ErrorState`, `SelectSessionView`) with state-driven routing.

Purpose: Deliver full session creation capabilities (SESS-03) and Electron-parity zero/error/selection screens (STATE-02).
Output: Working Create Session dialog modal, directory picker integration, state views module, and state routing unit tests.
</objective>

<execution_context>
@C:/Users/alyza/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/alyza/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-rest-client-sidebar-session-management/02-CONTEXT.md
@.planning/phases/02-rest-client-sidebar-session-management/02-RESEARCH.md
@.planning/phases/02-rest-client-sidebar-session-management/02-UI-SPEC.md
@.planning/phases/02-rest-client-sidebar-session-management/02-VALIDATION.md
@.planning/phases/02-rest-client-sidebar-session-management/02-01-PLAN.md
@desktop-gpui/crates/webtmux/src/app_state.rs
@desktop-gpui/crates/webtmux/src/views/sidebar.rs
</context>

<assumption_delta_decision>
no-change: Modal dialog mechanics, native directory picker, and workspace state pages adhere 1:1 to UI-SPEC contracts and Go REST endpoints.
</assumption_delta_decision>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement ST1 workspace state pages (EmptyState, ErrorState, SelectSessionView)</name>
  <files>
    desktop-gpui/crates/webtmux/src/views/mod.rs,
    desktop-gpui/crates/webtmux/src/views/session_states.rs,
    desktop-gpui/crates/webtmux/src/app_state.rs,
    desktop-gpui/crates/webtmux/tests/state_view_test.rs
  </files>
  <read_first>
    E:/Coding Stuff/web-tmux/.planning/phases/02-rest-client-sidebar-session-management/02-RESEARCH.md,
    E:/Coding Stuff/web-tmux/.planning/phases/02-rest-client-sidebar-session-management/02-UI-SPEC.md,
    E:/Coding Stuff/web-tmux/fe/src/features/sessions/EmptyState.tsx,
    E:/Coding Stuff/web-tmux/fe/src/features/sessions/ErrorState.tsx,
    E:/Coding Stuff/web-tmux/fe/src/features/sessions/SelectSessionView.tsx
  </read_first>
  <behavior>
    - Unit test `test_workspace_state_routing`: verifies that when `tree_error` is present -> routes to `ErrorState`; when `tree.sessions.is_empty()` -> routes to `EmptyState`; when sessions exist but `active_session.is_none()` -> routes to `SelectSessionView`; when `active_session` is set -> routes to active session placeholder (STATE-02 per D-03).
  </behavior>
  <acceptance_criteria>
    - `views/session_states.rs` implements three views matching ST1 spec:
      * `EmptyState`: centered column (p-8, gap-3), 40x40 `TerminalSquare` icon (#808080), Heading 16px/500 "No tmux sessions", Body 14px/400 "Create your first session to get started.", primary CTA "Create Session" (#d4d4d4 fill, #1e1e1e text).
      * `ErrorState`: centered column (p-8, gap-3), 40x40 `AlertTriangle` icon (#7F1D1D), Heading 16px/500 "Tmux is not installed", Body 14px/400 "Tmux GUI requires tmux (>= 3.2) on Linux. Install it and try again." (max-w-sm), outline CTA "Retry" (triggers immediate `trigger_poll`).
      * `SelectSessionView`: centered column (px-6 py-10), 48x48 rounded-xl hero box with 24x24 `SquareTerminal`, Heading 16px/500 "No session open", Body 14px/400 description (max-w-sm), max-w-md 2-column session card grid with session names and `{n}w` window count badges (#2d2d2d badge bg). Clicking card sets `active_session`.
    - `AppState::render` routes the workspace body to the appropriate view based on `tree_error`, `tree.sessions`, and `active_session`.
  </acceptance_criteria>
  <action>
    Create `desktop-gpui/crates/webtmux/src/views/session_states.rs` implementing `render_empty_state`, `render_error_state`, `render_select_session_view`, and `render_workspace_body`.
    Register `session_states` in `views/mod.rs`.
    Integrate `render_workspace_body` into `AppState::render` in `crates/webtmux/src/app_state.rs`.
    Write and run unit test in `desktop-gpui/crates/webtmux/tests/state_view_test.rs`.
  </action>
  <verify>
    <automated>cargo test -p webtmux test_workspace_state_routing</automated>
    <fails_when>non-zero exit code or "test result: FAILED" in summary</fails_when>
  </verify>
  <done>
    All three workspace state pages (EmptyState, ErrorState, SelectSessionView) are implemented with 1:1 UI-SPEC styling and state routing test passes green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement DLG1 CreateSessionDialog with gpui-component Dialog, InputState, and App::prompt_for_paths</name>
  <files>
    desktop-gpui/crates/webtmux/src/views/mod.rs,
    desktop-gpui/crates/webtmux/src/views/create_session_dialog.rs,
    desktop-gpui/crates/webtmux/src/app_state.rs,
    desktop-gpui/crates/webtmux/src/views/sidebar.rs
  </files>
  <read_first>
    E:/Coding Stuff/web-tmux/.planning/phases/02-rest-client-sidebar-session-management/02-RESEARCH.md,
    E:/Coding Stuff/web-tmux/.planning/phases/02-rest-client-sidebar-session-management/02-UI-SPEC.md,
    E:/Coding Stuff/web-tmux/fe/src/features/sessions/CreateSessionDialog.tsx
  </read_first>
  <acceptance_criteria>
    - `views/create_session_dialog.rs` implements `open_create_session_dialog(window: &mut Window, cx: &mut App)` using `window.open_dialog` (DLG1):
      * 440px width, 8px radius, `#1e1e1e` background, 1px `#3c3c3c` border, 20px padding, dark overlay backdrop.
      * Header: Title "Create Session" 16px/500, Description "Start a new tmux session." 12px/400.
      * Fields: Name (required, placeholder "dev", validated with `ValidateSessionName`), Working directory (optional, placeholder "~/projects (optional)", with 32x32 Browse button using `FolderOpen` icon), Initial command (optional, placeholder "e.g. nvim (optional)").
      * Browse directory button invokes `cx.prompt_for_paths(PathPromptOptions { directories: true, files: false, multiple: false, prompt: Some("Select Working Directory".into()) })` and populates the directory input on selection without modifying cwd on cancel.
      * Inline error line: rendered in destructive `#7F1D1D` styling when backend returns 400/409 error.
      * Footer: Cancel button (outline) dismissing dialog, Create button (primary `#d4d4d4` fill) disabled when name is empty or submission is busy.
    - Submitting sends `POST /api/sessions`, on 200/201 closes dialog, calls `trigger_poll`, and sets new session active.
    - Sidebar `Plus` button, EmptyState "Create Session" button, and SelectSessionView triggers all open the dialog.
  </acceptance_criteria>
  <action>
    Create `desktop-gpui/crates/webtmux/src/views/create_session_dialog.rs`:
    - Manage dialog form state entity holding `Entity<InputState>` for name, directory, initial_command, `is_submitting: bool`, and `error_message: Option<String>`.
    - Implement directory picker handler using `cx.prompt_for_paths` updating the directory `InputState`.
    - Implement submit handler calling `rest_client.create_session(&req)` asynchronously, updating error message on failure or closing dialog and selecting session on success.
    - Connect triggers from `views/sidebar.rs` (Plus button), `views/session_states.rs` (EmptyState CTA), and AppState.
    - Register `create_session_dialog` in `views/mod.rs`.
  </action>
  <verify>
    <automated>cargo check --manifest-path desktop-gpui/Cargo.toml --package webtmux</automated>
    <fails_when>non-zero exit code or compilation error in webtmux crate</fails_when>
  </verify>
  <done>
    CreateSessionDialog is fully integrated with text inputs, native directory browsing, client/server validation error display, and session creation triggers.
  </done>
</task>

<task type="auto">
  <name>Task 3: Full workspace verification and end-to-end integration test sweep</name>
  <files>
    desktop-gpui/crates/webtmux/src/app_state.rs,
    desktop-gpui/crates/webtmux/tests/state_view_test.rs,
    desktop-gpui/crates/webtmux/tests/polling_test.rs
  </files>
  <read_first>
    E:/Coding Stuff/web-tmux/.planning/phases/02-rest-client-sidebar-session-management/02-VALIDATION.md
  </read_first>
  <acceptance_criteria>
    - Full workspace test suite `cargo test --manifest-path desktop-gpui/Cargo.toml --workspace` passes cleanly.
    - All requirements for Phase 2 (SHELL-03, SESS-01, SESS-03, SESS-06, STATE-02) are covered by automated unit/integration tests or clear manual verification paths.
  </acceptance_criteria>
  <action>
    Run complete workspace tests and verify that all crates compile without warnings.
    Execute integration tests covering REST parsing, session name validation, polling generation guard, sidebar toggle snap, and state view routing.
  </action>
  <verify>
    <automated>cargo test --manifest-path desktop-gpui/Cargo.toml --workspace</automated>
    <fails_when>non-zero exit code or "test result: FAILED" in summary</fails_when>
  </verify>
  <done>
    All workspace tests pass green across all crates in desktop-gpui.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User Input -> Create Session Form | User enters session name, directory path, initial command |
| Native File System -> Path Picker | File system paths selected via OS platform dialog |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-04 | Elevation / Path Traversal | `CreateSessionDialog` (cwd input) | low | accept | Directory picker requires explicit user gesture via native OS prompt; backend runs under local user context |
| T-02-05 | Information Disclosure | `views/session_states` (ErrorState) | low | accept | Display high-level missing tmux guidance without exposing internal filesystem paths or tokens |
| T-02-06 | Denial of Service | `CreateSessionDialog` (double-submit) | medium | mitigate | Form disables Submit button while `is_submitting` is true to prevent duplicate concurrent POST requests |
| T-02-SC | Tampering | Cargo dependencies | high | mitigate | Workspace pinned dependencies maintained without ad-hoc external crates |
</threat_model>

<verification>
Run the workspace test suite:
```bash
cargo test --manifest-path desktop-gpui/Cargo.toml --workspace
```
</verification>

<success_criteria>
1. User can open Create Session dialog, type session name, pick working directory via native picker, and create session (SESS-03).
2. Backend rejection errors display inline in destructive styling without crashing dialog.
3. EmptyState, ErrorState, and SelectSessionView render matching Electron styling and copy (STATE-02).
4. Entire workspace test suite passes green.
</success_criteria>

<output>
Create `.planning/phases/02-rest-client-sidebar-session-management/02-02-PLAN.md` when done
</output>
