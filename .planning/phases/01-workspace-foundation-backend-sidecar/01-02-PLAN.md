---
phase: 01-workspace-foundation-backend-sidecar
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - desktop-gpui/crates/settings/Cargo.toml
  - desktop-gpui/crates/settings/src/lib.rs
  - desktop-gpui/crates/settings/src/paths.rs
  - desktop-gpui/crates/settings/tests/store_test.rs
  - desktop-gpui/crates/webtmux/src/window_state.rs
  - desktop-gpui/crates/webtmux/src/views/tab_strip.rs
  - desktop-gpui/crates/webtmux/tests/window_state.rs
autonomous: true
requirements:
  - SET-05
  - SHELL-02

estimate:
  tokens: 50000
  raw_tokens: 25000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Settings save/load run twice on the same input yields identical state; corrupt-recovery re-run returns defaults again and never panics (unit tests: roundtrip, corrupt_recovery)"
    - { statement: "settings save is atomic (write temp, rename); an interrupted save leaves the old or new file intact, and load falls back to .bak/defaults", verification: "backstop" }
    - "Window position, size, and maximized state persist across restarts using the DesktopSettings JSON store with corrupt .bak recovery"
    - "User can minimize, maximize/restore, and close the application via custom title bar window controls"
    - "Window control buttons (40x32px) display idle #9d9d9d icons, hover #2d2d2d fill with #d4d4d4 icon, and close button hover #7F1D1D fill with #ffffff icon"
    - "Maximize and restore icon states swap between Square and Copy 14px icons based on actual window maximized state via IsZoomed / is_window_maximized"
    - "Title bar drag region uses WindowControlArea::Drag enabling native OS move, double-click maximize toggle, and Aero snap"
    - "Window state restore clamps width and height away from 0 and handles minimized negative coordinate anomalies without breaking launch"
  prohibitions:
    - "No settings dir collision: config directory is strictly isolated to 'tmux-gui-desktop' and does not write to 'tmux-gui-gtk' or 'webterm-desktop'"
    - "No secret custody in settings: no encryption key fields or secret storage in Phase 1 settings JSON"
  artifacts:
    - path: "desktop-gpui/crates/settings/src/lib.rs"
      provides: "DesktopSettings struct, WindowState struct, Theme enum, load/save API with .bak corrupt-file recovery"
      exports: ["DesktopSettings", "WindowState", "Theme", "SettingsError"]
    - path: "desktop-gpui/crates/settings/src/paths.rs"
      provides: "Platform-specific configuration directory paths resolving to tmux-gui-desktop"
      exports: ["config_dir", "settings_file_path"]
    - path: "desktop-gpui/crates/webtmux/src/window_state.rs"
      provides: "Window geometry restoration, boundary clamping, toggle_maximize (Win32 ShowWindowAsync/DWM), observe close-flush persistence"
      exports: ["restore", "extract_window_state", "toggle_maximize", "is_window_maximized", "observe"]
    - path: "desktop-gpui/crates/webtmux/src/views/tab_strip.rs"
      provides: "Interactive title bar with min/max/restore/close button click handlers, tooltips, and WindowControlArea::Drag"
  key_links:
    - from: "desktop-gpui/crates/webtmux/src/main.rs"
      to: "desktop-gpui/crates/settings/src/lib.rs"
      via: "DesktopSettings::load loads persisted settings before window open"
      pattern: "DesktopSettings::load"
    - from: "desktop-gpui/crates/webtmux/src/main.rs"
      to: "desktop-gpui/crates/webtmux/src/window_state.rs"
      via: "window_state::restore computes initial WindowBounds and window_state::observe hooks close flush"
      pattern: "window_state::restore"
    - from: "desktop-gpui/crates/webtmux/src/views/tab_strip.rs"
      to: "desktop-gpui/crates/webtmux/src/window_state.rs"
      via: "Maximize/restore button calls toggle_maximize"
      pattern: "toggle_maximize"
---

<objective>
Implement the `webtmux-settings` crate with atomic JSON persistence, corrupt-file `.bak` recovery, and first-run defaults, alongside the `webtmux` window state and interactive title-bar controls (minimize, maximize/restore with `IsZoomed` toggle, close, and `WindowControlArea::Drag`).

Purpose: Satisfy requirements SET-05 and SHELL-02 by enabling persistent window geometry across restarts and complete custom title-bar window management.
Output: Tested `webtmux-settings` crate, `window_state.rs` module, and interactive `views/tab_strip.rs` with full unit test coverage.
</objective>

<execution_context>
@C:/Users/alyza/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/alyza/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-workspace-foundation-backend-sidecar/01-CONTEXT.md
@.planning/phases/01-workspace-foundation-backend-sidecar/01-RESEARCH.md
@.planning/phases/01-workspace-foundation-backend-sidecar/01-UI-SPEC.md
@.planning/phases/01-workspace-foundation-backend-sidecar/01-VALIDATION.md
@.planning/phases/01-workspace-foundation-backend-sidecar/01-01-PLAN.md
</context>

<assumption_delta_decision>
no-change: settings schema and window state persistence adhere to the proven web-term settings architecture, isolated to the tmux-gui-desktop configuration directory.
</assumption_delta_decision>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement webtmux-settings crate with .bak recovery and unit tests</name>
  <files>
    desktop-gpui/crates/settings/Cargo.toml,
    desktop-gpui/crates/settings/src/lib.rs,
    desktop-gpui/crates/settings/src/paths.rs,
    desktop-gpui/crates/settings/tests/store_test.rs
  </files>
  <read_first>
    E:/Coding Stuff/web-tmux/.planning/phases/01-workspace-foundation-backend-sidecar/01-RESEARCH.md,
    E:/Coding Stuff/web-tmux/.planning/phases/01-workspace-foundation-backend-sidecar/01-01-PLAN.md
  </read_first>
  <behavior>
    - Unit test `roundtrip`: saving a DesktopSettings struct with custom window geometry and theme, then loading from same path returns equal struct.
    - Unit test `corrupt_recovery`: if settings.json contains malformed JSON, load_from renames it to settings.json.bak, generates default DesktopSettings, saves defaults, and returns Ok(defaults) without error or panic.
    - Unit test `first_run`: loading from non-existent directory returns default DesktopSettings with custom_base populated and subsequent save creates settings.json.
  </behavior>
  <acceptance_criteria>
    - `desktop-gpui/crates/settings/src/paths.rs` uses folder name `tmux-gui-desktop` under `dirs::config_dir()`.
    - `DesktopSettings` struct contains fields: `backend_path: Option<PathBuf>`, `theme: Theme`, `window_state: Option<WindowState>`, `last_backend_url: Option<String>`, and `#[serde(skip)] pub custom_base: Option<PathBuf>`.
    - `WindowState` struct has `x: Option<i32>`, `y: Option<i32>`, `width: Option<u32>`, `height: Option<u32>`, `maximized: bool` with `#[serde(default)]`.
    - `Theme` enum has `Dark`, `Light`, `System` variants (defaulting to `Dark`).
    - `SettingsError` covers `Io` and `Json` errors.
    - `save` and `save_to` perform atomic write using temp file rename, setting 0600 permissions on Unix.
    - `load` and `load_from` implement corrupt file recovery: `file_path.with_extension("json.bak")` rename and defaults fallback.
    - All tests in `tests/store_test.rs` pass.
  </acceptance_criteria>
  <action>
    Implement `webtmux-settings` per RESEARCH Pattern 2:
    - In `crates/settings/Cargo.toml`: declare serde (derive), serde_json, dirs, thiserror, tempfile (dev).
    - In `src/paths.rs`: implement `config_dir()` -> `dirs::config_dir().join("tmux-gui-desktop")` and `config_dir_with_base(&Path)`.
    - In `src/lib.rs`: define `Theme`, `WindowState`, `DesktopSettings`, `SettingsError`. Implement `load()`, `load_from(&Path)`, `save()`, `save_to(&Path)`. Include `.bak` rename logic upon `serde_json::from_str` error.
    - In `tests/store_test.rs`: write test cases for `roundtrip`, `corrupt_recovery` (asserting `settings.json.bak` exists), and `first_run`.
  </action>
  <verify>
    <automated>cargo test -p webtmux-settings</automated>
    <fails_when>non-zero exit code or "test result: FAILED" in summary</fails_when>
  </verify>
  <done>
    `webtmux-settings` passes all unit tests for roundtrip persistence, corrupt .bak recovery, and first-run defaults.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement window_state restoration and interactive custom title-bar controls</name>
  <files>
    desktop-gpui/crates/webtmux/src/window_state.rs,
    desktop-gpui/crates/webtmux/src/views/tab_strip.rs,
    desktop-gpui/crates/webtmux/tests/window_state.rs
  </files>
  <read_first>
    E:/Coding Stuff/web-tmux/.planning/phases/01-workspace-foundation-backend-sidecar/01-RESEARCH.md,
    E:/Coding Stuff/web-tmux/.planning/phases/01-workspace-foundation-backend-sidecar/01-UI-SPEC.md,
    E:/Coding Stuff/web-tmux/.planning/phases/01-workspace-foundation-backend-sidecar/01-01-PLAN.md
  </read_first>
  <behavior>
    - Unit test `window_state_guards`: `restore` rejects degenerate 0x0 sizes, clamps width/height (min 800x500), filters out minimized negative coordinates (<= -10000), and restores `WindowBounds::Maximized` when maximized is true while preserving normal bounds.
    - Unit test `extract_window_state`: preserves previous windowed bounds when current state is maximized.
  </behavior>
  <acceptance_criteria>
    - `window_state.rs` exports `restore(&DesktopSettings) -> Option<WindowBounds>`, `extract_window_state(&Window, Option<&WindowState>) -> WindowState`, `is_window_maximized(&Window) -> bool` (using `IsZoomed` on Windows), `toggle_maximize(&mut Window) -> bool` (using `ShowWindowAsync` + DWM refresh on Windows / `window.zoom_window()` on other platforms), and `observe(&mut Window, Arc<Mutex<DesktopSettings>>, &mut Context)`.
    - `views/tab_strip.rs` wires interactive click handlers for:
      * Minimize: `window.minimize_window()`.
      * Maximize/Restore: calls `toggle_maximize(window)`, updates icon between `Square` and `Copy` based on `is_window_maximized(window)`.
      * Close: `window.remove_window()`.
      * Drag region: `.window_control_area(WindowControlArea::Drag)` on title bar background.
    - Window control button styling adheres to 01-UI-SPEC S1: 40x32px, radius 6px, idle icon `#9d9d9d`, hover `#2d2d2d` with `#d4d4d4` icon, close hover `#7F1D1D` with `#ffffff` icon.
    - `tests/window_state.rs` test suite passes.
  </acceptance_criteria>
  <action>
    Implement window management and controls:
    - In `crates/webtmux/src/window_state.rs`:
      * Port `restore`, `extract_window_state`, `is_window_maximized`, `toggle_maximize`, `observe` from web-term reference with web-tmux geometry numbers (1200x800 default, 800x500 min).
      * Implement `observe` using `window.on_window_should_close` to flush the extracted window state into `DesktopSettings` and call `settings.save()`.
    - In `crates/webtmux/src/views/tab_strip.rs`:
      * Connect `on_mouse_down(MouseButton::Left, ...)` handlers for minimize (`minimize_window`), maximize/restore (`toggle_maximize` + `is_window_maximized`), and close (`remove_window`).
      * Render title-bar drag region using `.window_control_area(WindowControlArea::Drag)`.
      * Apply hover colors: `#2d2d2d` for minimize/maximize, `#7F1D1D` for close button.
    - In `crates/webtmux/tests/window_state.rs`:
      * Write unit tests covering boundary clamping, minimized coordinate rejection, degenerate size fallback, and maximized state handling.
  </action>
  <verify>
    <automated>cargo test -p webtmux</automated>
    <fails_when>non-zero exit code or "test result: FAILED" in summary</fails_when>
  </verify>
  <done>
    `window_state` tests pass, `DesktopSettings` integrates with window lifecycle close-flush, and custom title bar window controls are fully functional.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Settings File -> Memory | Loading serialized JSON from user profile directory |
| OS Window Manager -> GPUI Window | Win32 / X11 window placement and maximization events |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-06 | Denial of Service | `webtmux-settings::load` | medium | mitigate | Corrupt settings JSON triggers automatic rename to `.json.bak` and regenerates default settings instead of crashing |
| T-01-07 | Tampering / Corruption | `webtmux-settings::save` | medium | mitigate | Atomic file write via temporary file creation and replacement to prevent partial write corruption |
| T-01-08 | Information Disclosure | `webtmux-settings` file permissions | low | mitigate | Restrict file permissions to 0600 on Unix systems; rely on user profile ACLs on Windows |
| T-01-09 | Denial of Service | `webtmux::window_state::restore` | low | mitigate | Clamp invalid, zero, or minimized negative window coordinates to safe defaults (min 800x500) |
</threat_model>

<verification>
Run settings and window state test suites:
```bash
cargo test -p webtmux-settings
cargo test -p webtmux --test window_state
```
</verification>

<success_criteria>
1. `webtmux-settings` roundtrip, corrupt recovery, and first run unit tests pass.
2. `window_state` restoration correctly clamps coordinates and restores maximized bounds.
3. Custom title bar controls (minimize, maximize/restore, close, drag) are fully wired according to 01-UI-SPEC.
</success_criteria>

<output>
Create `.planning/phases/01-workspace-foundation-backend-sidecar/01-02-PLAN.md` when done
</output>
