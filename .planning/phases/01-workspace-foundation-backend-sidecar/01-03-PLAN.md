---
phase: 01-workspace-foundation-backend-sidecar
plan: "03"
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - desktop-gpui/tools/fxc/main.rs
  - desktop-gpui/tools/fxc/README.md
  - desktop-gpui/docs/BUILDING.md
  - .gitignore
autonomous: true
requirements:
  - PKG-01

estimate:
  tokens: 40000
  raw_tokens: 20000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`cargo build` succeeds on Windows in debug mode without requiring fxc, and release builds succeed with tools/fxc compiled and GPUI_FXC_PATH configured"
    - "Linux build prerequisites (fontconfig, xkbcommon, X11/Wayland dev libraries, Vulkan loader) are fully documented in desktop-gpui/docs/BUILDING.md"
    - "The fxc tool compiles cleanly via rustc into a standalone executable that invokes d3dcompiler_47.dll to satisfy gpui-pre-windows release shader compilation"
    - "Git ignore rules properly exclude desktop-gpui build artifacts (target/, tools/fxc/*.exe) and test-support binaries"
  prohibitions:
    - "No requirement for full Windows SDK: release builds function on standard Windows installations via the ported tools/fxc drop-in"
  artifacts:
    - path: "desktop-gpui/tools/fxc/main.rs"
      provides: "Zero-dependency Rust CLI wrapping Windows d3dcompiler_47.dll to emulate fxc.exe for gpui-pre release shader compilation"
    - path: "desktop-gpui/tools/fxc/README.md"
      provides: "Documentation and build command for the fxc shader compiler tool"
    - path: "desktop-gpui/docs/BUILDING.md"
      provides: "Comprehensive build instructions for Windows (debug & release with fxc) and Linux (apt package prerequisites)"
  key_links:
    - from: "desktop-gpui/tools/fxc/main.rs"
      to: "C:/Windows/System32/d3dcompiler_47.dll"
      via: "Dynamically loads D3DCompile from system DLL to compile HLSL shaders when GPUI_FXC_PATH is set"
      pattern: "d3dcompiler_47.dll"
---

<objective>
Port the zero-dependency `tools/fxc` HLSL shader compiler tool from the web-term reference to enable Windows release builds (`cargo build --release`), create the comprehensive `desktop-gpui/docs/BUILDING.md` guide covering Windows and Linux build prerequisites, and verify git ignore hygiene.

Purpose: Satisfy requirement PKG-01 by ensuring reliable debug and release builds on Windows and clear setup documentation for Linux environments.
Output: `tools/fxc/main.rs`, `tools/fxc/README.md`, `desktop-gpui/docs/BUILDING.md`, and updated `.gitignore`.
</objective>

<execution_context>
@C:/Users/alyza/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/alyza/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-workspace-foundation-backend-sidecar/01-CONTEXT.md
@.planning/phases/01-workspace-foundation-backend-sidecar/01-RESEARCH.md
@.planning/phases/01-workspace-foundation-backend-sidecar/01-VALIDATION.md
@.planning/phases/01-workspace-foundation-backend-sidecar/01-01-PLAN.md
@.planning/phases/01-workspace-foundation-backend-sidecar/01-02-PLAN.md
</context>

<assumption_delta_decision>
no-change: fxc tool and building documentation are brownfield ports from the proven web-term implementation, ensuring zero extra external build dependencies for Windows release compilation.
</assumption_delta_decision>

<tasks>

<task type="auto">
  <name>Task 1: Port tools/fxc HLSL shader compiler and verify Windows release build</name>
  <files>
    desktop-gpui/tools/fxc/main.rs,
    desktop-gpui/tools/fxc/README.md,
    .gitignore
  </files>
  <read_first>
    E:/Coding Stuff/web-tmux/.planning/phases/01-workspace-foundation-backend-sidecar/01-RESEARCH.md
  </read_first>
  <acceptance_criteria>
    - `desktop-gpui/tools/fxc/main.rs` is a self-contained, zero-crate Rust file that loads `d3dcompiler_47.dll` and parses standard `fxc.exe` command-line arguments (`/T`, `/E`, `/Fh`, `/Vn`, `/nologo`).
    - `desktop-gpui/tools/fxc/README.md` documents how `fxc.exe` is compiled using `rustc -O main.rs -o fxc.exe` and how `GPUI_FXC_PATH` is configured.
    - `.gitignore` ignores `desktop-gpui/target/`, `desktop-gpui/tools/fxc/*.exe`, `desktop-gpui/test-support/`, and `*.bak`.
    - Compiling `fxc.exe` via `rustc` succeeds.
    - `cargo build --release --manifest-path desktop-gpui/Cargo.toml --package webtmux` succeeds when `GPUI_FXC_PATH` is pointed to the compiled `fxc.exe`.
  </acceptance_criteria>
  <action>
    Port `tools/fxc` from web-term reference:
    - Create `desktop-gpui/tools/fxc/main.rs`: implement command-line parsing for `/T` (target profile), `/E` (entrypoint), `/Fh` (header output), `/Vn` (variable name), `/nologo`, loading `d3dcompiler_47.dll` using `LoadLibraryA` / `GetProcAddress` to invoke `D3DCompile`, and writing the output C header file.
    - Create `desktop-gpui/tools/fxc/README.md` explaining the purpose (satisfying `gpui-pre-windows` release build requirements without full Windows SDK) and compilation instructions.
    - Update root `.gitignore` to exclude `desktop-gpui/target/`, `desktop-gpui/tools/fxc/fxc.exe`, `desktop-gpui/tools/fxc/*.exe`, and test fixtures.
    - Compile `desktop-gpui/tools/fxc/main.rs` using `rustc -O` and test release compilation of `webtmux`.
  </action>
  <verify>
    <automated>powershell -Command "rustc -O desktop-gpui/tools/fxc/main.rs -o desktop-gpui/tools/fxc/fxc.exe; $env:GPUI_FXC_PATH=(Resolve-Path desktop-gpui/tools/fxc/fxc.exe); cargo build --release --manifest-path desktop-gpui/Cargo.toml --package webtmux"</automated>
    <fails_when>non-zero exit code or compilation error in release mode</fails_when>
  </verify>
  <done>
    `tools/fxc` compiles, `fxc.exe` successfully produces compiled shaders during release build, and `cargo build --release` produces `target/release/webtmux.exe`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author BUILDING.md documentation for Windows and Linux environments</name>
  <files>
    desktop-gpui/docs/BUILDING.md
  </files>
  <read_first>
    E:/Coding Stuff/web-tmux/.planning/phases/01-workspace-foundation-backend-sidecar/01-RESEARCH.md,
    E:/Coding Stuff/web-tmux/.planning/phases/01-workspace-foundation-backend-sidecar/01-VALIDATION.md
  </read_first>
  <acceptance_criteria>
    - `desktop-gpui/docs/BUILDING.md` exists and details:
      1. Prerequisites for Windows (Rust toolchain, Go toolchain for sidecar, MSVC Build Tools).
      2. Windows Debug build instructions (`cargo build`).
      3. Windows Release build instructions (compiling `tools/fxc/fxc.exe` and setting `GPUI_FXC_PATH`).
      4. Prerequisites for Linux (apt-get packages: `libfontconfig1-dev`, `libwayland-dev`, `libxkbcommon-dev`, `libxkbcommon-x11-dev`, `libx11-dev`, `libxcursor-dev`, `libxrandr-dev`, `libgl1-mesa-dev`, `vulkan-loader`, `tmux`).
      5. Linux build commands (`cargo build` / `cargo build --release`).
      6. Running tests (`cargo test --workspace`).
  </acceptance_criteria>
  <action>
    Create `desktop-gpui/docs/BUILDING.md` documenting the complete developer setup and build instructions for both Windows and Linux platforms as researched.
  </action>
  <verify>
    <automated>powershell -Command "if (Test-Path desktop-gpui/docs/BUILDING.md) { Get-Content desktop-gpui/docs/BUILDING.md | Select-String -Pattern 'libfontconfig1-dev', 'GPUI_FXC_PATH' } else { exit 1 }"</automated>
    <fails_when>missing file or required package / env var keywords not found</fails_when>
  </verify>
  <done>
    `desktop-gpui/docs/BUILDING.md` is written and complete.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build Tooling -> System Libraries | Dynamic loading of OS DLL `d3dcompiler_47.dll` during release build |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-10 | Tampering / Supply Chain | `desktop-gpui/tools/fxc` | low | mitigate | Zero external crates; minimal direct Win32 API calls to system `d3dcompiler_47.dll` |
</threat_model>

<verification>
Verify fxc compilation, release build, and doc contents:
```powershell
rustc -O desktop-gpui/tools/fxc/main.rs -o desktop-gpui/tools/fxc/fxc.exe
$env:GPUI_FXC_PATH = (Resolve-Path desktop-gpui/tools/fxc/fxc.exe)
cargo build --release --manifest-path desktop-gpui/Cargo.toml --package webtmux
```
</verification>

<success_criteria>
1. `tools/fxc` compiles and enables Windows release builds without requiring a full Windows SDK.
2. `cargo build --release` produces an optimized release binary of `webtmux`.
3. `BUILDING.md` documents all required dependencies and build steps for Windows and Linux.
</success_criteria>

<output>
Create `.planning/phases/01-workspace-foundation-backend-sidecar/01-03-PLAN.md` when done
</output>
