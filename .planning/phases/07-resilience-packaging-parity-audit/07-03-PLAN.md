---
phase: 07-resilience-packaging-parity-audit
plan: "03"
type: execute
wave: 2
depends_on:
  - "07-01"
  - "07-02"
files_modified:
  - desktop-gpui/crates/webtmux/src/views/pane_grid.rs
  - desktop-gpui/crates/webtmux/src/views/pane_context_menu.rs
  - desktop-gpui/crates/webtmux/src/views/dialogs.rs
  - desktop-gpui/crates/webtmux/tests/parity_audit_test.rs
autonomous: true
requirements:
  - STATE-03
  - PKG-02
  - PKG-03

estimate:
  tokens: 55000
  raw_tokens: 27500
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The parity checklist table (5 daily-driver scenarios × all folded HV rows) is fully ticked against Electron with screenshot refs, showing 1:1 with only the documented sidebar-snap deviation (SC4, D6)"
    - "Every deferred HV advisory from Phases 1/2/3/4/5/6 has a checklist row and a pass/fail verdict — none silently dropped (D6)"
    - "Theme-swap audit ran under default-dark + 2 non-default themes (one light, one saturated dark); preset tokens were threaded ONLY into surfaces the audit flagged as visibly divergent (D7)"
    - "Pane grid/headers, context menus, and kill/rename dialogs read preset tokens per render wherever the audit flagged divergence — the 06-VERIFICATION §10 deferral is closed (D7)"
    - "Full workspace suite (116 + all Phase 7 tests) is green with untouched lockfiles at milestone close"
    - { statement: "Side-by-side pixels (vim/htop, rapid switching, resize flood, rename mid-session, theme swap) are indistinguishable from Electron to human eyes", verification: "backstop" }
  prohibitions:
    - "No blind re-theme of all chrome files — audit-gated threading only (D7)"
    - "No re-arguing accepted deviations (sidebar snap, SelectSession trigger override, pane.join D8, DWM dark frame, ctrl-only palette, EXTRA-01/02/03) — record, do not re-litigate"
    - "No protocol forks in the GPUI crate for audit findings — backend gaps logged as backend issues (milestone rule)"
    - "No eyeball theme bucketing — Dark/Light uses the ported themeLuminance rule"
  artifacts:
    - path: "desktop-gpui/crates/webtmux/tests/parity_audit_test.rs"
      provides: "Headless regression guards for audit-fixed surfaces (theme-token reads, guard invariants)"
    - path: "desktop-gpui/crates/webtmux/src/views/pane_grid.rs"
      provides: "Preset-token chrome ONLY on audit-flagged surfaces (D7-gated)"
  key_links:
    - from: ".planning/phases/07-resilience-packaging-parity-audit/07-VALIDATION.md"
      to: "desktop-gpui/crates/webtmux/tests/parity_audit_test.rs"
      via: "Every automated checklist row maps to a named test; manual-only rows cite eyes-on procedure"
      pattern: "parity_audit"
---

<objective>
Close the milestone: execute the SC4 side-by-side parity audit vs Electron (D6 checklist —
5 daily-driver scenarios × ALL folded HV advisories from Phases 1–6), perform D7-gated
chrome-threading fixes only where the audit shows visible divergence (closing the
06-VERIFICATION §10 deferral), and leave the workspace green with the checklist ticked.

Purpose: This is the plan that earns the "looks like Electron" claim — every deferred
human-verification advisory ever recorded lands here, and the hardcoded-chrome residual
gets fixed if and only if eyes see divergence. Output: ticked checklist, gated fixes with
headless guards, green workspace, milestone-close SUMMARY.
</objective>

<execution_context>
@C:/Users/alyza/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/alyza/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-resilience-packaging-parity-audit/07-CONTEXT.md
@.planning/phases/07-resilience-packaging-parity-audit/07-RESEARCH.md
@.planning/phases/07-resilience-packaging-parity-audit/07-VALIDATION.md
@.planning/phases/06-theme-settings-chrome-parity/06-VERIFICATION.md
@desktop-gpui/crates/webtmux/src/views/pane_grid.rs
@desktop-gpui/crates/webtmux/src/views/pane_context_menu.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the theme-swap audit pass and thread preset tokens into flagged surfaces only</name>
  <files>
    desktop-gpui/crates/webtmux/src/views/pane_grid.rs,
    desktop-gpui/crates/webtmux/src/views/pane_context_menu.rs,
    desktop-gpui/crates/webtmux/src/views/dialogs.rs,
    desktop-gpui/crates/webtmux/tests/parity_audit_test.rs
  </files>
  <read_first>
    E:/Coding Stuff/web-tmux/.planning/phases/07-resilience-packaging-parity-audit/07-RESEARCH.md,
    E:/Coding Stuff/web-tmux/.planning/phases/06-theme-settings-chrome-parity/06-VERIFICATION.md,
    E:/Coding Stuff/web-tmux/desktop-gpui/crates/webtmux/src/views/pane_grid.rs,
    E:/Coding Stuff/web-tmux/desktop-gpui/crates/webtmux/src/views/pane_context_menu.rs
  </read_first>
  <behavior>
    - Test `test_flagged_chrome_reads_preset_tokens`: for each audit-flagged surface, the render path resolves colors through the active UiThemePreset helper (no `rgb(0x…)` literal on the flagged path) — unflagged files keep their literals untouched.
    - Test `test_theme_swap_convergence_model`: switching preset mid-session re-resolves all threaded surfaces (model-level: preset read per render, no cached stale colors).
  </behavior>
  <acceptance_criteria>
    - Audit pass runs theme-swap scenario A5 under default-dark + 2 non-default themes (one light, one saturated dark) while panes stream; each visibly divergent surface is recorded with (file, line, screenshot ref) before any edit (D7).
    - ONLY flagged surfaces are threaded, using the established per-render preset-read pattern (55-read precedent); hardcoded literals elsewhere are explicitly left alone.
    - Dark/Light bucketing uses the ported `themeLuminance` rule, never eyeballing.
  </acceptance_criteria>
  <action>
    Audit first: exercise the A5 theme-swap drill under the three themes and list divergent surfaces with evidence. Then thread preset tokens into flagged surfaces only (pane grid/headers, context menus, kill/rename dialogs as flagged — e.g. pane_grid.rs:72, pane_context_menu.rs:238 literals). Write parity_audit_test.rs guards for each threaded surface. If the audit shows zero divergence on a file, leave it hardcoded and record why.
  </action>
  <verify>
    <automated>cargo test -p webtmux --test parity_audit_test</automated>
    <fails_when>non-zero exit code or "test result: FAILED" in summary</fails_when>
  </verify>
  <done>
    D7 deferral closed honestly: flagged surfaces threaded + guarded, unflagged surfaces documented as-is; parity_audit_test green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Execute the folded-HV checklist rows (daily-driver A1–A4 + residuals B)</name>
  <files>
    desktop-gpui/crates/webtmux/tests/parity_audit_test.rs
  </files>
  <read_first>
    E:/Coding Stuff/web-tmux/.planning/phases/07-resilience-packaging-parity-audit/07-RESEARCH.md,
    E:/Coding Stuff/web-tmux/.planning/phases/07-resilience-packaging-parity-audit/07-VALIDATION.md,
    E:/Coding Stuff/web-tmux/.planning/phases/02-rest-client-sidebar-session-management/02-VERIFICATION.md,
    E:/Coding Stuff/web-tmux/.planning/phases/06-theme-settings-chrome-parity/06-VERIFICATION.md
  </read_first>
  <behavior>
    - Test `test_generation_guard_regression`: rapid-switch-under-churn model never applies a stale session snapshot (Ph3 HV-4 headless guard).
    - Test `test_rename_migration_model`: rename success migrates sidebar+tabs with no ghost windows (Ph3 HV-3 / Ph5 HV-4 model).
    - Test `test_kill_gate_model`: kill gates honor switches off→direct / on→verbatim dialog (Ph5 HV-5 / Ph6 HV-7 model).
  </behavior>
  <acceptance_criteria>
    - Every folded-HV row from RESEARCH § Parity Audit Checklist (A1 vim/htop, A2 rapid switching, A3 resize flood, A4 rename mid-session, B sidebar/tree, dialogs, state pages, terminal I/O, workspace ops, settings/palette/font, failed-backend loop) gets a pass/fail verdict with screenshot ref recorded in the SUMMARY.
    - Headless-guardable rows gain model tests in parity_audit_test.rs; pixel-only rows are marked eyes-on with procedure (GPUI not headless-renderable precedent).
    - Accepted deviations (registry items 1–6) are recorded once, never re-audited or re-argued.
  </behavior>
  <action>
    Work the checklist row by row against live Electron + GPUI side-by-side: vim/htop alt-screen parity, churn-switching, resize/divider floods, mid-session renames, binary dialog/palette drills, failed-backend Retry/Quit loop (also exercising the 07-01 transport-lost path). Add headless model guards where possible; record pixel verdicts with screenshot refs. Log any backend gap as a backend issue, never a GPUI fork.
  </action>
  <verify>
    <automated>cargo test -p webtmux --test parity_audit_test</automated>
    <fails_when>non-zero exit code or "test result: FAILED" in summary</fails_when>
  </verify>
  <done>
    All checklist rows have verdicts; model guards green; only documented deviations remain.
  </done>
</task>

<task type="auto">
  <name>Task 3: Milestone-close validation sweep (full green + lockfile gate + checklist sign-off)</name>
  <files>
    desktop-gpui/crates/webtmux/tests/parity_audit_test.rs
  </files>
  <read_first>
    E:/Coding Stuff/web-tmux/.planning/phases/07-resilience-packaging-parity-audit/07-VALIDATION.md,
    E:/Coding Stuff/web-tmux/.planning/phases/07-resilience-packaging-parity-audit/07-01-SUMMARY.md,
    E:/Coding Stuff/web-tmux/.planning/phases/07-resilience-packaging-parity-audit/07-02-SUMMARY.md
  </read_first>
  <acceptance_criteria>
    - `cargo test --manifest-path desktop-gpui/Cargo.toml --workspace` fully green (116 baseline + all Phase 7 tests, 0 failed) from the repo root with the Phase-6 build env (`CARGO_TARGET_DIR=C:\cargo-target\web-tmux`, `CARGO_BUILD_JOBS=2`).
    - `git diff --stat` over `Cargo.toml Cargo.lock desktop-gpui/Cargo.toml desktop-gpui/Cargo.lock` is empty (milestone zero-new-package invariant).
    - SUMMARY records the final checklist table state, the human_needed remainder (Linux build/smoke), and the milestone-close handoff for `/gsd-verify-work 7`.
  </acceptance_criteria>
  <action>
    Run the full workspace suite as the final regression sweep (re-executes Phases 1–6 suites like the Phase-6 verifier did); assert the lockfile gate; reconcile the checklist table to fully ticked-or-human_needed; write the milestone-close SUMMARY with verdicts, screenshot refs, deviation links, and handoff notes.
  </action>
  <verify>
    <automated>cargo test --manifest-path desktop-gpui/Cargo.toml --workspace</automated>
    <fails_when>non-zero exit code or "test result: FAILED" in summary</fails_when>
  </verify>
  <done>
    Workspace fully green, lockfiles untouched, checklist closed; milestone ready for /gsd-verify-work 7.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Audit-driven edits → chrome render | Threading touches user-visible trust surfaces (dialog copy, kill gates) |
| `git diff` lockfile gate | Guards the milestone dependency invariant at close |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-06 | Tampering | Kill-gate dialogs (`dialogs.rs`, D7 edits) | high | mitigate | Threading edits must not alter kill-gate copy or switch semantics — verbatim copy preserved, covered by the kill-gate model test; any copy change fails the audit row |
| T-07-07 | Spoofing | Theme-threaded chrome | low | mitigate | Preset-token reads change colors only, never labels/copy; themeLuminance bucketing (no eyeball buckets that could mislabel dark/light affordances) |
| T-07-SC | Tampering | npm/pip/cargo installs | high | mitigate | Zero new packages — close-out task asserts empty `git diff` on Cargo.toml/Cargo.lock |
</threat_model>

<verification>
Run the audit guards then the full close-out sweep:
```bash
cargo test -p webtmux --test parity_audit_test
cargo test --manifest-path desktop-gpui/Cargo.toml --workspace
```
Manual-only (eyes-on, recorded in SUMMARY): A1–A5 side-by-side pixels, dialog typing/picker
live drills, resize-flood observation, theme-swap flash check — GPUI not headless-renderable.
</verification>

<success_criteria>
1. Parity checklist fully ticked with 1:1 verdicts + screenshot refs; only the sidebar-snap deviation stands (SC4).
2. All folded HV advisories (Phases 1/2/3/4/5/6) have verdicts; none dropped (SC4).
3. D7 chrome-threading closed: flagged surfaces threaded + guarded, rest documented (SC4).
4. Workspace fully green with untouched lockfiles; human_needed remainder (Linux) handed off.
</success_criteria>

<output>
Create `.planning/phases/07-resilience-packaging-parity-audit/07-03-SUMMARY.md` when done
</output>
