---
phase: 07-resilience-packaging-parity-audit
plan: "01"
type: tracer
wave: 1
depends_on: []
files_modified:
  - desktop-gpui/crates/backend-client/src/ws.rs
  - desktop-gpui/crates/webtmux/src/app_state.rs
  - desktop-gpui/crates/webtmux/src/views/mod.rs
  - desktop-gpui/crates/webtmux/src/views/reconnect_banner.rs
  - desktop-gpui/crates/webtmux/src/views/toasts.rs
  - desktop-gpui/crates/webtmux/tests/reconnect_test.rs
  - desktop-gpui/crates/webtmux/tests/toast_test.rs
autonomous: true
requirements:
  - STATE-03

estimate:
  tokens: 60000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Killing the backend socket shows 'Connection lost — retrying… (attempt N)' with auto-retry, while a server-sent tmux.disconnected shows 'tmux disconnected — waiting for tmux' with manual Reconnect and NO client retry (strict taxonomy per D2)"
    - "tmux.reconnecting renders the transient amber 'Reconnecting to tmux…' banner, and recovery clears the banner with a 'Reconnected' toast (SC1)"
    - "Failing commands surface backend-verbatim error toasts; toast queue caps at 5 with dedupe-by-key so reconnect flaps never cover the workspace (D1/D2)"
    - "Pump close/error arms synthesize ONLY the local transport-lost signal — zero EV_TMUX_DISCONNECTED references remain in those arms (Pitfall 1 grep-gate)"
    - "Auto-reconnect timing matches FE BACKOFF [250,500,1000,2000,5000,10000]ms verbatim and stale retry timers die on rename/close/re-resolve (D3)"
    - { statement: "Banner/toast pixels, kill-tmux vs kill-backend drill, and flap behavior look correct side-by-side with Electron", verification: "backstop" }
  prohibitions:
    - "No rendering banner copy directly from TransportState without the origin bit (reintroduces the conflation)"
    - "No client-side retry of server-sent tmux.disconnected (double-retry with the backend monitor)"
    - "No unbounded toast growth (every push path covered by the cap test)"
    - "No blocking the GPUI thread on connect (connect_async stays on TOKIO_RT)"
  artifacts:
    - path: "desktop-gpui/crates/webtmux/src/views/reconnect_banner.rs"
      provides: "Reconnect banner view with 4-way taxonomy copy + manual Reconnect button"
      exports: ["render_reconnect_banner"]
    - path: "desktop-gpui/crates/webtmux/src/views/toasts.rs"
      provides: "Bounded toast overlay view (cap 5, dedupe, autohide model)"
      exports: ["render_toasts", "Toast", "ToastKind"]
    - path: "desktop-gpui/crates/webtmux/src/app_state.rs"
      provides: "Toast queue + transport-lost mapping + BACKOFF retry timers in AppState"
    - path: "desktop-gpui/crates/backend-client/src/ws.rs"
      provides: "Pump-local transport-lost signal distinct from EV_TMUX_DISCONNECTED"
  key_links:
    - from: "desktop-gpui/crates/backend-client/src/ws.rs"
      to: "desktop-gpui/crates/webtmux/src/app_state.rs"
      via: "Pump forwards transport.lost (never EV_TMUX_DISCONNECTED) into apply_event"
      pattern: "transport.lost"
    - from: "desktop-gpui/crates/webtmux/src/views/reconnect_banner.rs"
      to: "desktop-gpui/crates/webtmux/src/app_state.rs"
      via: "Banner reads transport + origin bit and dispatches manual reconnect"
      pattern: "transport_origin"
---

<objective>
Deliver the STATE-03 resilience tracer: split transport-lost from tmux-health at the pump
boundary (D2, per D-decision), render the reconnect banner + bounded toast overlay end to
end, and arm FE-verbatim BACKOFF auto-reconnect with generation guards (D3).

Purpose: The pump conflation (ws.rs:437-443 synthesizing EV_TMUX_DISCONNECTED on any socket
close) is the single design constraint every SC1 copy string keys off — splitting it first
unblocks all banner/toast/retry work and proves the event→state→view path before expansion.
Output: Split pump signal, banner + toast views wired into render, retry timers, headless
contract tests, workspace green.
</objective>

<execution_context>
@C:/Users/alyza/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/alyza/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-resilience-packaging-parity-audit/07-CONTEXT.md
@.planning/phases/07-resilience-packaging-parity-audit/07-RESEARCH.md
@.planning/phases/07-resilience-packaging-parity-audit/07-UI-SPEC.md
@.planning/phases/07-resilience-packaging-parity-audit/07-VALIDATION.md
@desktop-gpui/crates/backend-client/src/ws.rs
@desktop-gpui/crates/webtmux/src/app_state.rs
@desktop-gpui/crates/webtmux/src/bundle.rs
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Split transport-lost from tmux-health and wire the reconnect banner end to end</name>
  <files>
    desktop-gpui/crates/backend-client/src/ws.rs,
    desktop-gpui/crates/webtmux/src/app_state.rs,
    desktop-gpui/crates/webtmux/src/views/mod.rs,
    desktop-gpui/crates/webtmux/src/views/reconnect_banner.rs,
    desktop-gpui/crates/webtmux/tests/reconnect_test.rs
  </files>
  <read_first>
    E:/Coding Stuff/web-tmux/.planning/phases/07-resilience-packaging-parity-audit/07-RESEARCH.md,
    E:/Coding Stuff/web-tmux/desktop-gpui/crates/backend-client/src/ws.rs,
    E:/Coding Stuff/web-tmux/desktop-gpui/crates/webtmux/src/app_state.rs,
    E:/Coding Stuff/web-tmux/fe/src/features/panes/PaneWorkspace.tsx,
    E:/Coding Stuff/web-tmux/fe/src/lib/websocket.ts
  </read_first>
  <behavior>
    - Test `test_transport_lost_distinct_from_tmux_disconnected`: pump close-synthesis constant differs from the server `tmux.disconnected` constant; `apply_event` maps each to a distinct (transport, origin) pair per D2.
    - Test `test_tmux_reconnecting_maps_transient`: server `tmux.reconnecting` maps to Reconnecting WITHOUT arming a client retry timer (monitor owns that retry).
    - Test `test_backoff_ladder_verbatim`: BACKOFF const equals [250,500,1000,2000,5000,10000]ms.
  </behavior>
  <acceptance_criteria>
    - Pump close/error arms forward a pump-local signal (e.g. `transport.lost`) that the server can never send; `grep EV_TMUX_DISCONNECTED` in those arms returns zero (Pitfall 1 gate).
    - `apply_event` maps transport-lost → `Disconnected` + armed BACKOFF retry, `tmux.disconnected` → `Disconnected` WITHOUT client retry, `tmux.reconnecting` → `Reconnecting` transient — each carrying an origin bit the banner reads (never bare `TransportState`).
    - `views/reconnect_banner.rs` renders the D2 copy table + manual Reconnect button on the disconnected states, stacked above the workspace via the existing `.when()` overlay pattern; registered in `views/mod.rs` and called from `AppState::render`.
  </acceptance_criteria>
  <action>
    Introduce the pump-local transport-lost signal per RESEARCH Pattern 1 (never serialize it; handle only in apply_event). Extend AppState transport state with the origin bit and BACKOFF const per D2/D3. Create reconnect_banner.rs per 07-UI-SPEC BANNER-1 geometry and wire it into AppState::render above the workspace. Write reconnect_test.rs covering the mapping + ladder contracts. Never block the GPUI thread on connect; keep connect_async on TOKIO_RT.
  </action>
  <verify>
    <automated>cargo test -p webtmux --test reconnect_test</automated>
    <fails_when>non-zero exit code or "test result: FAILED" in summary</fails_when>
  </verify>
  <done>
    Single happy path proven end to end: socket kill → transport-lost banner + armed retry; server tmux.disconnected → distinct copy with manual Reconnect; reconnect_test green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bounded toast overlay queue with taxonomy copy and error surfaces</name>
  <files>
    desktop-gpui/crates/webtmux/src/app_state.rs,
    desktop-gpui/crates/webtmux/src/views/toasts.rs,
    desktop-gpui/crates/webtmux/tests/toast_test.rs
  </files>
  <read_first>
    E:/Coding Stuff/web-tmux/.planning/phases/07-resilience-packaging-parity-audit/07-RESEARCH.md,
    E:/Coding Stuff/web-tmux/.planning/phases/07-resilience-packaging-parity-audit/07-UI-SPEC.md,
    E:/Coding Stuff/web-tmux/desktop-gpui/crates/webtmux/src/app_state.rs,
    E:/Coding Stuff/web-tmux/fe/src/lib/commands.ts
  </read_first>
  <behavior>
    - Test `test_toast_queue_cap_and_dedupe`: pushing 7 toasts keeps the newest 5; same (kind+session) key collapses instead of duplicating (Pitfall 3).
    - Test `test_toast_copy_keys`: each D2 taxonomy arm resolves its fixed copy string; command.error toast carries the backend-verbatim message.
    - Test `test_command_error_surfaces_toast`: a correlated `command.error` / 10s timeout through `note_session_error` paths enqueues an error toast (inline `last_error` lines stay untouched).
  </behavior>
  <acceptance_criteria>
    - Hand-rolled `VecDeque` queue (cap 5, drop-oldest, dedupe key per kind+session, autohide 5s errors / 3s success [A1]) per D1 — no gpui-component Root adoption.
    - `views/toasts.rs` renders the overlay stack per 07-UI-SPEC TOAST-1; toasts render backend strings as plain text with kind icon prefix (STRIDE T-07-01).
    - Recovery pushes "Reconnected" success toast and clears the banner; `server.error` pushes an error toast (Open Q2).
  </acceptance_criteria>
  <action>
    Add the toast queue model + push/dedupe/expire helpers to AppState; create toasts.rs overlay and stack it with the palette-overlay `.when()` pattern. Wire command.error/timeout/server.error paths to push error toasts with backend-verbatim text while keeping dialog/toolbar inline errors. Write toast_test.rs for cap/dedupe/copy contracts.
  </action>
  <verify>
    <automated>cargo test -p webtmux --test toast_test</automated>
    <fails_when>non-zero exit code or "test result: FAILED" in summary</fails_when>
  </verify>
  <done>
    Toast overlay renders the full taxonomy; flap-safe by construction (cap+dedupe tested); toast_test green.
  </done>
</task>

<task type="auto">
  <name>Task 3: Generation-guarded BACKOFF retry loop with manual Reconnect and regression sweep</name>
  <files>
    desktop-gpui/crates/webtmux/src/app_state.rs,
    desktop-gpui/crates/webtmux/tests/reconnect_test.rs
  </files>
  <read_first>
    E:/Coding Stuff/web-tmux/.planning/phases/07-resilience-packaging-parity-audit/07-RESEARCH.md,
    E:/Coding Stuff/web-tmux/desktop-gpui/crates/webtmux/src/app_state.rs,
    E:/Coding Stuff/web-tmux/fe/src/lib/websocket.ts
  </read_first>
  <behavior>
    - Test `test_retry_timer_bound_to_generation`: a retry armed at generation N is dropped when the tab sits at generation N+1 (rename/close/re-resolve); banner attempt count increments per ladder step.
    - Regression: existing `ws_guard_test` suite stays green (no STATE-04 regression).
  </behavior>
  <acceptance_criteria>
    - Retry chain spawns on TOKIO_RT with the verbatim BACKOFF ladder, captures (session, generation) at arm time, drops on mismatch, tails at 10s forever like FE (Open Q3); banner shows attempt N and always offers manual Reconnect (close+ensure path, FE `reconnectSession` parity).
    - `ws_guard_test` + full workspace suite green; `git diff --stat` over `Cargo.toml Cargo.lock desktop-gpui/Cargo.toml desktop-gpui/Cargo.lock` is empty (zero-new-package gate).
  </acceptance_criteria>
  <action>
    Implement the generation-guarded retry timer chain on the transport-lost arm only, reusing the ensure_session_socket outcome path (which already re-captures panes via D6 recapture_session). Wire the banner Reconnect button to the manual close+ensure path. Extend reconnect_test.rs with the generation-drop test; run the ws_guard regression and the full workspace suite plus the lockfile gate.
  </action>
  <verify>
    <automated>cargo test --manifest-path desktop-gpui/Cargo.toml --workspace</automated>
    <fails_when>non-zero exit code or "test result: FAILED" in summary</fails_when>
  </verify>
  <done>
    Retry loop matches FE timing with generation safety; STATE-03 headless contracts green; workspace 116+ new tests green with untouched lockfiles.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Go backend WS → GPUI pump (`ws.rs`) | Server-originated event frames + error strings cross here (loopback, single-user) |
| Toast/banner render | Backend-verbatim strings reach pixels here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Spoofing | `views/toasts.rs`, `views/reconnect_banner.rs` | medium | mitigate | Render backend-verbatim error text as plain text elements only (never markup/commands); prefix every toast with its kind icon so origin is visually typed |
| T-07-02 | Denial of Service | Toast queue (`app_state.rs`) | medium | mitigate | Hard cap 5 + dedupe-by-key + autohide so 250ms-ladder flaps cannot cover the workspace (tested in toast_test) |
| T-07-03 | Denial of Service | Retry timers (`app_state.rs`) | medium | mitigate | Verbatim BACKOFF ladder with 10s tail (no hot spin) + generation capture/drop so zombie timers never reconnect renamed/closed tabs |
| T-07-SC | Tampering | npm/pip/cargo installs | high | mitigate | Zero new packages this plan — workspace-suite task asserts empty `git diff` on Cargo.toml/Cargo.lock |
</threat_model>

<verification>
Run the resilience contract suites, the STATE-04 regression, then the full workspace gate:
```bash
cargo test -p webtmux --test reconnect_test
cargo test -p webtmux --test toast_test
cargo test -p webtmux --test ws_guard_test
cargo test --manifest-path desktop-gpui/Cargo.toml --workspace
```
</verification>

<success_criteria>
1. WS-drop and tmux-event banners are distinguishable with correct retry behavior per D2 (SC1).
2. Command errors surface as toasts with backend-verbatim text; queue is flap-safe (SC1).
3. Retry timing matches FE BACKOFF verbatim with generation safety; ws_guard regression green (SC1).
4. Banner/toast pixels drill (kill-tmux vs kill-backend) → human UAT rows in 07-03 audit (backstop).
</success_criteria>

<output>
Create `.planning/phases/07-resilience-packaging-parity-audit/07-01-SUMMARY.md` when done
</output>
