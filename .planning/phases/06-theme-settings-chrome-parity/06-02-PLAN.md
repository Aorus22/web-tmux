---
phase: 06-theme-settings-chrome-parity
plan: "02"
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - desktop-gpui/crates/backend-client/src/rest.rs
  - desktop-gpui/crates/backend-client/tests/binary_test.rs
  - desktop-gpui/crates/webtmux/src/actions.rs
  - desktop-gpui/crates/webtmux/src/views/palette.rs
  - desktop-gpui/crates/webtmux/src/views/mod.rs
  - desktop-gpui/crates/webtmux/src/views/terminal_view.rs
  - desktop-gpui/crates/webtmux/src/views/settings.rs
  - desktop-gpui/crates/webtmux/src/app_state.rs
  - desktop-gpui/crates/webtmux/tests/palette_test.rs
autonomous: true
requirements:
  - SET-03
  - SET-04
  - DLG-01
  - DLG-03
  - SHELL-05

estimate:
  tokens: 60000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The tmux binary path setting validates against POST /api/tmux/binary with Using {binary} ({version}) / error status surfaced in the Settings UI (criterion 3, SET-03 per D6)"
    - "Kill-confirmation dialogs honor the three per-surface settings toggles exactly like Electron's shouldConfirm dispatch (criterion 4 switch half, SET-04/DLG-03 per D7)"
    - "Dialog copy matches Electron verbatim and the three Switch rows persist round-trip through save/load (criterion 4, SET-04 per D7)"
    - "Ctrl+Shift+P opens a filterable command palette with exactly the 7 FE actions plus the Open Session group; Open Session opens the picked session (criterion 5 palette half, DLG-01 per D5)"
    - "Palette never loses to the focused terminal: ctrl+shift+P bubbles to the action instead of entering the pty (DLG-01 Pitfall 1 per D5)"
    - "Palette + binary status + switch rows render from the embedded font/icons with no system deps (criterion 5 chrome half, SHELL-05 per D8)"
    - { statement: "Palette actions fired with no applicable pane/session degrade to the same guards as their button/menu equivalents, never a raw backend error", verification: "backstop" }
  prohibitions:
    - "No per-keystroke tmux -V validation (explicit Check/Enter only — D6)"
    - "No hand-rolled palette list widget (vendored command module owns filter/focus/keyboard — D5)"
    - "No palette actions beyond the FE 7 + Open Session (extras are v2)"
    - "No client-side PATH probing or spawning of the tmux binary (client sends the string; backend probes)"
    - "No dialog-behavior changes (switches + copy audit only — D7)"
  artifacts:
    - path: "desktop-gpui/crates/backend-client/src/rest.rs"
      provides: "RestClient::set_tmux_binary POST plus existing TmuxInfo model reuse"
      exports: ["set_tmux_binary"]
    - path: "desktop-gpui/crates/webtmux/src/actions.rs"
      provides: "TogglePalette action + keybinding registration"
      exports: ["TogglePalette", "bind_palette_keys"]
    - path: "desktop-gpui/crates/webtmux/src/views/palette.rs"
      provides: "Command-palette dialog (7 actions + Open Session) over existing sends"
      exports: ["render_palette"]
    - path: "desktop-gpui/crates/webtmux/tests/palette_test.rs"
      provides: "Palette group/filter/guard model contracts (if factored pure)"
      exports: ["test_palette_groups", "test_palette_filter"]
  key_links:
    - from: "desktop-gpui/crates/webtmux/src/views/settings.rs"
      to: "desktop-gpui/crates/backend-client/src/rest.rs"
      via: "Check/Enter invokes set_tmux_binary; status line renders Using version or backend error string"
      pattern: "set_tmux_binary"
    - from: "desktop-gpui/crates/webtmux/src/views/palette.rs"
      to: "desktop-gpui/crates/webtmux/src/app_state.rs"
      via: "Action items dispatch existing correlated sends; Open Session calls open_session"
      pattern: "submit_"
    - from: "desktop-gpui/crates/webtmux/src/views/terminal_view.rs"
      to: "desktop-gpui/crates/webtmux/src/actions.rs"
      via: "on_key_down early-returns ctrl+shift+P so the keystroke reaches TogglePalette"
      pattern: "TogglePalette"
---

<objective>
Complete Phase 6 on top of the 06-01 tracer: tmux-binary validation against the backend
(SET-03), the Ctrl+Shift+P command palette on the vendored command module (DLG-01), and the
three kill-confirm switches with the FE copy audit (SET-04/DLG-03).

Purpose: Deliver every remaining chrome-parity surface — validated binary setting, filterable
palette over existing mutations, per-surface kill toggles — reusing the 06-01 theme/settings
foundation.
Output: Binary REST method + settings UX, palette action/view + key routing, kill switches,
and binary/palette headless tests.
</objective>

<execution_context>
@C:/Users/alyza/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/alyza/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-theme-settings-chrome-parity/06-CONTEXT.md
@.planning/phases/06-theme-settings-chrome-parity/06-RESEARCH.md
@.planning/phases/06-theme-settings-chrome-parity/06-UI-SPEC.md
@.planning/phases/06-theme-settings-chrome-parity/06-VALIDATION.md
@.planning/phases/06-theme-settings-chrome-parity/06-01-PLAN.md
@desktop-gpui/crates/webtmux/src/app_state.rs
@desktop-gpui/crates/webtmux/src/views/settings.rs
@desktop-gpui/crates/webtmux/src/views/terminal_view.rs
@desktop-gpui/crates/backend-client/src/rest.rs
@fe/src/features/palette/CommandPalette.tsx
</context>

<assumption_delta_decision>
no-change: Binary contract, palette item lists, kill labels/copy, and guard semantics are
verbatim copies of the Read-verified FE and be/ sources via 06-RESEARCH D5–D7.
</assumption_delta_decision>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add set_tmux_binary REST method plus Settings validation UX</name>
  <files>
    desktop-gpui/crates/backend-client/src/rest.rs,
    desktop-gpui/crates/backend-client/tests/binary_test.rs,
    desktop-gpui/crates/webtmux/src/views/settings.rs,
    desktop-gpui/crates/webtmux/src/app_state.rs
  </files>
  <read_first>
    E:/Coding Stuff/web-tmux/.planning/phases/06-theme-settings-chrome-parity/06-RESEARCH.md,
    E:/Coding Stuff/web-tmux/be/internal/server/health.go,
    E:/Coding Stuff/web-tmux/be/internal/tmux/binary.go,
    E:/Coding Stuff/web-tmux/fe/src/lib/api.ts,
    E:/Coding Stuff/web-tmux/fe/src/features/settings/TerminalSettings.tsx,
    E:/Coding Stuff/web-tmux/desktop-gpui/crates/backend-client/src/rest.rs
  </read_first>
  <behavior>
    - Unit test `test_set_tmux_binary_shape`: mock HTTP stub asserts POST body is exactly `{path}` and a 200 `{binary, version, ok}` parses into `TmuxInfo`; a 400 `{"error": "tmux binary %q is not usable: …"}` surfaces via `extract_error_message` (SET-03 per D6).
    - Unit test `test_binary_status_copy`: pure status-line helper renders `Using {binary} ({version})` on ok and the raw error string on failure (never a generic code).
  </behavior>
  <acceptance_criteria>
    - `RestClient::set_tmux_binary(path: &str) -> Result<TmuxInfo, RestError>` posts `{path}` to `{base}/api/tmux/binary`, mirroring the `create_session` error-extraction shape (no new error type, no new crate).
    - Settings page Terminal section gains the tmux-binary input + Check-on-Enter/button + status line; validation fires on explicit Check/Enter only (never per-keystroke); FE copy `"Choose the same tmux installation…"` preserved.
    - Apply-once-on-ready: after backend connects, the stored `tmux_binary` (if non-empty) posts once (FE `App.tsx:120-127` parity).
    - `AppState` holds the binary status (`Option<Result<TmuxInfo-ish, String>>` shape at the executor's discretion) so poll re-renders never clear it.
  </acceptance_criteria>
  <action>
    Implement the REST method per the research code example — copy the `create_session` POST/error pattern, do not invent a second HTTP idiom. Wire the settings UX into the 06-01 section anchors (do not rebuild the page). Test against an in-process mock `TcpListener` stub (no live backend needed); the live-backend interop check is a Phase-7 HV item. Never spawn or probe the path client-side — the string is opaque until the backend answers.
  </action>
  <verify>
    <automated>cargo test -p webtmux-backend-client binary_shape</automated>
    <fails_when>non-zero exit code or "test result: FAILED" in summary</fails_when>
  </verify>
  <done>
    set_tmux_binary posts the FE-shaped body, parses ok/error exactly, and the settings status line shows version or backend error; binary tests pass green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build Ctrl+Shift+P command palette on the vendored command module</name>
  <files>
    desktop-gpui/crates/webtmux/src/actions.rs,
    desktop-gpui/crates/webtmux/src/views/mod.rs,
    desktop-gpui/crates/webtmux/src/views/palette.rs,
    desktop-gpui/crates/webtmux/src/views/terminal_view.rs,
    desktop-gpui/crates/webtmux/src/app_state.rs,
    desktop-gpui/crates/webtmux/tests/palette_test.rs
  </files>
  <read_first>
    E:/Coding Stuff/web-tmux/.planning/phases/06-theme-settings-chrome-parity/06-RESEARCH.md,
    E:/Coding Stuff/web-tmux/fe/src/features/palette/CommandPalette.tsx,
    E:/Coding Stuff/web-tmux/fe/src/lib/commands.ts,
    E:/Coding Stuff/web-tmux/desktop-gpui/crates/webtmux/src/views/terminal_view.rs,
    E:/Coding Stuff/web-tmux/desktop-gpui/crates/webtmux/src/app_state.rs
  </read_first>
  <behavior>
    - Unit test `test_palette_groups`: pure item builder yields exactly the 7 FE action labels in FE order plus one entry per tree session in Open Session (DLG-01 per D5; extras fail the test).
    - Unit test `test_palette_filter`: substring filter matches label + keywords case-insensitively (e.g. "split" hits both splits, "kill" hits Kill Pane) — or documents manual-UAT if the model cannot be factored pure.
  </behavior>
  <acceptance_criteria>
    - `actions.rs` defines `TogglePalette` (+ per-action structs as needed) with `cx.bind_keys([KeyBinding::new("ctrl-shift-p", TogglePalette, None)])` following the reference `actions.rs:5-41` pattern; Wave-0 compile check confirms the keystroke string parses (A2).
    - `views/palette.rs` renders `Command`/`CommandState` over two `CommandGroup`s (Actions + Open Session) with `CommandItem::label/keywords/action/checked` builders; action items dispatch the existing correlated sends with FE guards mirrored (window actions need active session, pane actions need active pane — disabled/hidden otherwise); Open Session calls `open_session`.
    - `TerminalView::on_key_down` early-returns ctrl+shift(+platform)+`p` before pty conversion so the palette opens from terminal focus (Pitfall 1); sidebar/other focus paths keep working.
    - Before writing the view, the executor reads the vendored `command/command.rs` + `command/state.rs` render API (A3 Wave-0 read) and notes the signatures in the SUMMARY; if the module API does not fit a modal overlay, halt with PLANNING INCONCLUSIVE details instead of hand-rolling.
  </acceptance_criteria>
  <action>
    Assemble the palette from the vendored primitives per D5 — do not build a custom searchable list (anti-pattern). Mirror the FE group/item lists verbatim including keywords; reuse existing sends (fire-and-forget like chip clicks). Factor the item/filter model pure so the two behavior tests run headless; if purity is impossible, write the tests against whatever pure seam exists and log palette open/filter/select as Phase-7 HV items. Register modules in `views/mod.rs` and hold palette open-state on `AppState`.
  </action>
  <verify>
    <automated>cargo test -p webtmux palette_groups</automated>
    <fails_when>non-zero exit code or "test result: FAILED" in summary</fails_when>
  </verify>
  <done>
    Ctrl+Shift+P opens the palette from any focus, filter narrows both groups, actions dispatch through existing sends with FE guards, and palette tests pass green.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire three kill-confirm switches plus FE copy audit and phase gate</name>
  <files>
    desktop-gpui/crates/webtmux/src/views/settings.rs,
    desktop-gpui/crates/webtmux/src/app_state.rs
  </files>
  <read_first>
    E:/Coding Stuff/web-tmux/.planning/phases/06-theme-settings-chrome-parity/06-RESEARCH.md,
    E:/Coding Stuff/web-tmux/fe/src/features/settings/TerminalSettings.tsx,
    E:/Coding Stuff/web-tmux/fe/src/features/sessions/SessionContextMenu.tsx,
    E:/Coding Stuff/web-tmux/fe/src/lib/commands.ts,
    E:/Coding Stuff/web-tmux/desktop-gpui/crates/webtmux/src/app_state.rs,
    E:/Coding Stuff/web-tmux/desktop-gpui/crates/webtmux/src/views/settings.rs
  </read_first>
  <acceptance_criteria>
    - Settings page gains the three `Switch` rows with FE labels verbatim (`"Confirm before killing pane/window/session"`) persisting through existing `save()` and round-tripping through load (SET-04 per D7).
    - Copy audit complete: all three `Kill*Form` dialogs match FE strings (`Kill session "{name}"?`, `"This terminates the tmux session and all processes inside it."`, pane/window equivalents); any mismatch fixed in this task and listed in the SUMMARY; `shouldConfirm(kind)` dispatch shape matches FE with the three existing gates.
    - Toggling a switch changes the next kill flow immediately (dialog vs direct) with no restart and no other behavior change — dialog-behavior code untouched.
    - `cargo test --manifest-path desktop-gpui/Cargo.toml --workspace` fully green with zero new packages; light-theme DWM-frame deviation recorded for the Phase-7 audit (Pitfall 6).
  </acceptance_criteria>
  <action>
    Add only the three Switch rows into the 06-01 settings page (do not restyle dialogs). Diff every Kill dialog string against the FE sources line-by-line and fix copy drift; verify the gate dispatch against `commands.ts:40-50`. Persist via the existing `save()` path (apply-then-save synchronously in the same handler). Verify with the full workspace battery as the phase gate and enumerate the Phase-7 HV items (palette open/filter/select pixels, binary live interop, theme-swap flash, font render) in the SUMMARY.
  </action>
  <verify>
    <automated>cargo test --manifest-path desktop-gpui/Cargo.toml --workspace</automated>
    <fails_when>non-zero exit code or "test result: FAILED" in summary</fails_when>
  </verify>
  <done>
    Kill toggles honor per-surface settings exactly like Electron, dialog copy matches FE, and the full workspace suite is green — Phase 6 all five success criteria hold end-to-end (pixels to Phase 7).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Settings input → POST /api/tmux/binary | User-supplied path string crosses to the localhost backend |
| Palette action dispatch → WS correlated sends | Filtered user intent crosses into tmux mutations |
| Kill-switch booleans → destructive flows | Preference toggles gate session/window/pane destruction |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-04 | Tampering / Elevation | `set_tmux_binary` path input | high | mitigate | Client treats the path as an opaque string and never spawns it; backend allowlist-by-probe stores only `tmux -V`-passing executables under 3s timeout; backend is localhost-only; explicit Check/Enter (no keystroke-triggered process spawns) |
| T-06-05 | Denial of service | Palette pane/window actions | low | mitigate | FE guards mirrored (window actions need active session, pane actions need active pane); all targets ride existing typed envelopes the server re-validates |
| T-06-06 | Tampering | Kill-switch persistence | medium | mitigate | Switches persist via atomic save + `.bak` recovery; gates default-true so a missing/corrupt key keeps confirming instead of silently enabling destruction |
| T-06-SC | Tampering | Cargo dependencies | high | mitigate | Zero new packages; milestone-pinned workspace with committed `Cargo.lock` |
</threat_model>

<verification>
Run the single-filter binary and palette gates then the full workspace battery (reuse
`CARGO_TARGET_DIR=C:\cargo-target\web-tmux` with `CARGO_BUILD_JOBS=2..4`):
```bash
cargo test -p webtmux-backend-client binary_shape
cargo test -p webtmux palette_groups
cargo test --manifest-path desktop-gpui/Cargo.toml --workspace
```
Phase gate: full suite green + manual UAT class items (palette open/filter/select from
terminal focus, binary live validation, kill-toggle flows, theme-swap flash) recorded in
06-VALIDATION.md for the Phase-7 parity audit.
</verification>

<success_criteria>
1. Binary setting validates via POST /api/tmux/binary with version/error status in the UI (SET-03).
2. Ctrl+Shift+P palette filters 7 actions + Open Session and dispatches with FE guards (DLG-01).
3. Three kill switches persist and gate dialogs exactly like Electron with verbatim copy (SET-04/DLG-03).
4. Palette + status + switches render from embedded font/icons (SHELL-05).
5. Full workspace suite green with zero new packages.
</success_criteria>

<output>
Create `.planning/phases/06-theme-settings-chrome-parity/06-02-SUMMARY.md` when done
</output>
