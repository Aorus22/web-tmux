---
phase: 06-theme-settings-chrome-parity
plan: "01"
type: tracer
wave: 1
depends_on: []
files_modified:
  - scripts/generate-gpui-themes.mjs
  - desktop-gpui/crates/webtmux/src/themes_generated.rs
  - desktop-gpui/crates/webtmux/src/lib.rs
  - desktop-gpui/crates/webtmux/src/theme.rs
  - desktop-gpui/crates/webtmux/tests/theme_test.rs
  - desktop-gpui/crates/settings/src/lib.rs
  - desktop-gpui/crates/settings/tests/store_test.rs
  - desktop-gpui/crates/webtmux/src/app_state.rs
  - desktop-gpui/crates/webtmux/src/icons.rs
  - desktop-gpui/crates/webtmux/src/views/mod.rs
  - desktop-gpui/crates/webtmux/src/views/settings.rs
  - desktop-gpui/crates/webtmux/src/views/session_states.rs
  - desktop-gpui/crates/webtmux/src/views/tab_strip.rs
  - desktop-gpui/crates/webtmux/src/views/sidebar.rs
autonomous: true
requirements:
  - THEME-01
  - THEME-02
  - THEME-03
  - SET-01
  - SET-02
  - SHELL-04
  - SHELL-05

estimate:
  tokens: 60000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "All 102 UI + 78 terminal presets exist as checked-in generated rows; lengths test pins the counts so FE drift fails loudly (criterion 5 data half, THEME-01 per D1)"
    - "Every UI preset's terminalTheme link resolves to a real terminal preset; default-dark.background == 0x1e1e1e spot-check holds (THEME-01/THEME-02 per D1)"
    - "Picking a theme restyles all chrome live and terminals follow the linked preset; selection plus window geometry persist across restarts (criteria 1+5, THEME-02/SHELL-04 per D3)"
    - "Dark/light counterparts derive Dark/Light from the ported luminance rule with Electron colorScheme semantics; load-time re-sync fixes legacy theme/theme_preset skew (criterion 4 dark-semantics half, THEME-03 per D3)"
    - "Settings page shows the 3-column card grid with dark/light filter and '{n} themes available' (criterion 1, SET-01 per D4)"
    - "Terminal prefs (font size, line height, scrollback, per-pane TUI-scroll default) take effect immediately with FE clamps and persist (criterion 2, SET-02 per D2/D9)"
    - "All text renders from the embedded JetBrains Mono face and new chrome uses embedded lucide SVGs with no system deps (criterion 5 font/icons half, SHELL-05 per D8)"
    - { statement: "Rapid theme swaps mid-session never leave chrome and terminals on different presets", verification: "backstop" }
  prohibitions:
    - "No hand-transcribed preset colors (generator output only — D1)"
    - "No build-time Node dependency (output checked in; cargo build never runs the generator)"
    - "No cached colors in views (one preset read per render so cx.notify() repaints everything)"
    - "No terminal-override path (terminals always follow the linked preset — resolvedTerminalTheme parity)"
    - "No font-stack parsing (single embedded family; free text falls back — D8)"
  artifacts:
    - path: "scripts/generate-gpui-themes.mjs"
      provides: "TS → Rust generator (102 UI + 78 terminal rows + lookups + is_dark)"
      exports: ["generate-gpui-themes.mjs"]
    - path: "desktop-gpui/crates/webtmux/src/themes_generated.rs"
      provides: "Checked-in UI_THEMES x102 + TERMINAL_THEMES x78 + lookup helpers"
      exports: ["UI_THEMES", "TERMINAL_THEMES", "UiThemePreset", "TerminalThemePreset", "ui_preset_by_name", "terminal_preset_by_name", "terminal_preset_for_ui"]
    - path: "desktop-gpui/crates/webtmux/src/theme.rs"
      provides: "Preset-driven chrome color helpers replacing hardcoded rgb fallbacks"
      exports: ["apply_theme", "preset_bg", "preset_fg"]
    - path: "desktop-gpui/crates/webtmux/src/views/settings.rs"
      provides: "Settings page (Appearance card grid + filter, Terminal prefs section)"
      exports: ["render_settings"]
    - path: "desktop-gpui/crates/webtmux/tests/theme_test.rs"
      provides: "Headless lengths/link/fallback/luminance/filter contracts"
      exports: ["test_theme_table_lengths", "test_terminal_link_resolution", "test_get_ui_theme_fallback", "test_luminance_buckets", "test_theme_filter_counts"]
  key_links:
    - from: "desktop-gpui/crates/webtmux/src/app_state.rs"
      to: "desktop-gpui/crates/webtmux/src/themes_generated.rs"
      via: "set_theme_preset looks up UI preset, derives Dark/Light, pushes linked terminal palette to all terminal_views"
      pattern: "set_theme_preset"
    - from: "desktop-gpui/crates/webtmux/src/views/settings.rs"
      to: "desktop-gpui/crates/webtmux/src/app_state.rs"
      via: "Card click and pref edits call AppState setters that apply-then-save synchronously"
      pattern: "set_theme_preset"
    - from: "desktop-gpui/crates/webtmux/src/theme.rs"
      to: "desktop-gpui/crates/webtmux/src/themes_generated.rs"
      via: "Chrome helpers read the active preset per render with fallback to UI_THEMES[0]"
      pattern: "ui_preset_by_name"
---

<objective>
Deliver the Phase 6 tracer slice: generated 102+78 theme tables with headless count/link
contracts, extended settings model with back-compat defaults, `set_theme_preset` live-apply
pushing linked palettes to every terminal, preset-driven chrome helpers, and the full Settings
page (card grid + filter + terminal prefs) over the existing route with embedded font/icons.

Purpose: Prove the entire theme data pipeline end-to-end (FE TS → generator → checked-in Rust
→ live re-theme → persisted) before the palette/binary/switch wave builds on it.
Output: Generator script, checked-in theme tables, extended settings crate, live-apply path,
Settings page, icons, and green headless theme contracts.
</objective>

<execution_context>
@C:/Users/alyza/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/alyza/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-theme-settings-chrome-parity/06-CONTEXT.md
@.planning/phases/06-theme-settings-chrome-parity/06-RESEARCH.md
@.planning/phases/06-theme-settings-chrome-parity/06-UI-SPEC.md
@.planning/phases/06-theme-settings-chrome-parity/06-VALIDATION.md
@desktop-gpui/crates/webtmux/src/app_state.rs
@desktop-gpui/crates/webtmux/src/theme.rs
@desktop-gpui/crates/webtmux/src/icons.rs
@desktop-gpui/crates/webtmux/src/views/session_states.rs
@desktop-gpui/crates/settings/src/lib.rs
@fe/src/features/settings/data/ui-themes.ts
</context>

<assumption_delta_decision>
no-change: Preset colors, FE defaults, luminance rule, and settings-page shape are verbatim
ports of the Read-verified FE sources and web-term reference via 06-RESEARCH D1–D4/D8/D9.
</assumption_delta_decision>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Generate checked-in theme tables with headless count/link contracts</name>
  <files>
    scripts/generate-gpui-themes.mjs,
    desktop-gpui/crates/webtmux/src/themes_generated.rs,
    desktop-gpui/crates/webtmux/src/lib.rs,
    desktop-gpui/crates/webtmux/tests/theme_test.rs
  </files>
  <read_first>
    E:/Coding Stuff/web-tmux/.planning/phases/06-theme-settings-chrome-parity/06-RESEARCH.md,
    E:/Coding Stuff/web-tmux/fe/src/features/settings/data/ui-themes.ts,
    E:/Coding Stuff/web-tmux/fe/src/features/settings/data/terminal-themes.ts,
    E:/Coding Stuff/web-tmux/desktop-gpui/crates/webtmux/src/lib.rs,
    E:/Coding Stuff/web-tmux/desktop-gpui/crates/terminal/src/colors.rs
  </read_first>
  <behavior>
    - Unit test `test_theme_table_lengths`: `UI_THEMES.len() == 102` and `TERMINAL_THEMES.len() == 78` so an FE theme addition fails loudly (THEME-01 per D1).
    - Unit test `test_terminal_link_resolution`: every UI preset's `terminal_theme` name resolves in the terminal table (THEME-01/THEME-02 per D1).
    - Unit test `test_get_ui_theme_fallback`: unknown id returns `UI_THEMES[0]` (`getUiTheme` parity) and `default-dark.background == 0x1e1e1e` spot-check holds.
    - Unit test `test_luminance_buckets`: ported `themeLuminance` (WCAG weights, `> 0.5`) buckets known presets exactly like FE (`default-dark` dark, `default-light` light) (THEME-03 per D3).
    - Unit test `test_theme_filter_counts`: all/dark/light filter counts sum to 102 (SET-01 per D4).
  </behavior>
  <acceptance_criteria>
    - `scripts/generate-gpui-themes.mjs` regex-parses both TS data files and emits `themes_generated.rs` with `UiThemePreset` (name/label/`terminal_theme` + 15 `u32` colors + precomputed `is_dark`) and `TerminalThemePreset` (name/label + 19 `u32` colors) plus `ui_preset_by_name`, `terminal_preset_by_name`, `terminal_preset_for_ui` lookups; script header documents the manual regen step.
    - `themes_generated.rs` is checked in and registered in `lib.rs`; `cargo build` never invokes the generator (no build.rs, no new packages).
    - `tests/theme_test.rs` holds all five behavior tests above; full file compiles with zero new dependencies.
  </acceptance_criteria>
  <action>
    Write the generator per D1 (Node only, reads two in-repo files, writes one in-repo file — local-only supply chain). Run it once with node (or hand-port the output rows on a machine without node and note it in the SUMMARY) and check in the emitted `themes_generated.rs`. Register the module in `lib.rs`. Write the five headless tests first (RED), then the lookups/tables (GREEN) — do not hand-edit emitted color rows. If node is absent, fall back per A1 and record which path was taken.
  </action>
  <verify>
    <automated>cargo test -p webtmux theme_tables</automated>
    <fails_when>non-zero exit code or "test result: FAILED" in summary</fails_when>
  </verify>
  <done>
    Generator exists, checked-in tables hold 102 UI + 78 terminal rows with resolving links, and theme-table tests pass green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend DesktopSettings with back-compat defaults plus preset lookups</name>
  <files>
    desktop-gpui/crates/settings/src/lib.rs,
    desktop-gpui/crates/settings/tests/store_test.rs,
    desktop-gpui/crates/webtmux/src/theme.rs
  </files>
  <read_first>
    E:/Coding Stuff/web-tmux/.planning/phases/06-theme-settings-chrome-parity/06-RESEARCH.md,
    E:/Coding Stuff/web-tmux/desktop-gpui/crates/settings/src/lib.rs,
    E:/Coding Stuff/web-tmux/desktop-gpui/crates/settings/tests/store_test.rs,
    E:/Coding Stuff/web-tmux/desktop-gpui/crates/webtmux/src/theme.rs,
    E:/Coding Stuff/web-tmux/fe/src/stores/settingsStore.ts
  </read_first>
  <behavior>
    - Unit test `test_new_pref_defaults`: `DesktopSettings::default()` carries `tmux_binary ""`, `font_family "JetBrains Mono"`, `font_size 14.0`, `line_height 1.35`, `scrollback_lines 2000`, `tui_scroll_default true`, `theme_mode_filter "all"`, `theme_preset "default-dark"` (FE settingsStore.ts parity per D2).
    - Unit test `test_legacy_settings_back_compat`: legacy JSON without the new keys deserializes with the defaults above (mirrors the existing legacy test shape; SHELL-04 per D2).
    - Unit test `test_terminal_pref_clamps`: pure clamp helpers enforce font 8–32, line-height 1–2, scrollback 100–50000 (FE TerminalSettings.tsx parity per D9).
  </behavior>
  <acceptance_criteria>
    - `DesktopSettings` gains the seven fields with serde defaults (`default_true`-style fns where non-trivial); `Default` impl updated; geometry/`confirm_kill_*` fields untouched.
    - `tui_scroll()` falls back to `tui_scroll_default` instead of hardcoded `true`.
    - `theme.rs` keeps `apply_theme` and gains preset-driven chrome helpers (`bg/fg/card/border/muted/primary/…` reading the active preset with `UI_THEMES[0]` fallback) replacing the hardcoded `rgb(0x…)` literals.
    - Pure clamp helpers (`clamp_font_size`, `clamp_line_height`, `clamp_scrollback`) live where the settings view can reuse them.
  </acceptance_criteria>
  <action>
    Extend the settings struct additively per D2 — never rename existing fields. Extend `store_test.rs` with the three behavior tests (RED first). Add the `theme.rs` preset helpers reading `themes_generated` (Task-1 output; if Task 1 output is missing, halt — plan ordering guarantees it). Keep `apply_theme` signature unchanged so Phases 1–5 call sites compile untouched.
  </action>
  <verify>
    <automated>cargo test -p webtmux-settings kill_switches</automated>
    <fails_when>non-zero exit code or "test result: FAILED" in summary</fails_when>
  </verify>
  <done>
    Settings model holds all Phase 6 prefs with legacy back-compat, clamps match FE, and settings tests pass green.
  </done>
</task>

<task type="tracer">
  <name>Task 3: Wire set_theme_preset live-apply plus Settings page with font/icons</name>
  <files>
    desktop-gpui/crates/webtmux/src/app_state.rs,
    desktop-gpui/crates/webtmux/src/icons.rs,
    desktop-gpui/crates/webtmux/src/views/mod.rs,
    desktop-gpui/crates/webtmux/src/views/settings.rs,
    desktop-gpui/crates/webtmux/src/views/session_states.rs,
    desktop-gpui/crates/webtmux/src/views/tab_strip.rs,
    desktop-gpui/crates/webtmux/src/views/sidebar.rs
  </files>
  <read_first>
    E:/Coding Stuff/web-tmux/.planning/phases/06-theme-settings-chrome-parity/06-RESEARCH.md,
    E:/Coding Stuff/web-tmux/.planning/phases/06-theme-settings-chrome-parity/06-UI-SPEC.md,
    E:/Coding Stuff/web-tmux/desktop-gpui/crates/webtmux/src/app_state.rs,
    E:/Coding Stuff/web-tmux/desktop-gpui/crates/webtmux/src/views/session_states.rs,
    E:/Coding Stuff/web-tmux/desktop-gpui/crates/webtmux/src/views/terminal_view.rs,
    E:/Coding Stuff/web-tmux/fe/src/features/settings/UiThemeSettings.tsx
  </read_first>
  <acceptance_criteria>
    - `AppState::set_theme_preset(id, cx)` implements the D3 shape exactly: table lookup (fallback `[0]`), write `settings.theme_preset`, derive `settings.theme` Dark/Light from `is_dark`, `theme::apply_theme`, push `ColorPalette::from_rgb_u32(linked preset)` via `set_palette` to every live `terminal_views` entry + store as default for views created later, `cx.notify()`, synchronous `settings.save()`.
    - Startup path re-derives `settings.theme` from `theme_preset` `is_dark` and saves (Pitfall 2 stale-enum fix).
    - `views/settings.rs` replaces the placeholder behind `showing_settings`: max-w-672 centered column, Appearance section (filter row + `"{n} themes available"` + flex-wrap 3-column `w(px(204))` cards with 56px swatch / 3 dots / 2 bars / check overlay / click-to-apply), Terminal section (font-size/line-height/scrollback numeric inputs with clamps, TUI-scroll-default switch) — binary/tmux rows and kill switches arrive in 06-02, leave clearly-marked section anchors.
    - Chrome in `tab_strip.rs`, `sidebar.rs`, `session_states.rs`, pane headers reads the preset helpers (one read per render, no caching); FE copy strings preserved verbatim.
    - `icons.rs` appends `CHECK_SVG`, `SEARCH_SVG`, `PAINTBRUSH_SVG` (lucide, `stroke-width="1.5"`); font stays the embedded JetBrains Mono single family with an honest `"Font family (embedded: JetBrains Mono)"` label (D8).
    - Font size/family/line-height edits apply live via `set_font`/`set_font_size` on all live views + renderer defaults, then re-arm the debounced viewport (`arm_viewport_for_pane` + `schedule_debounced_resize`); scrollback edits recreate store `Terminal`s with the new limit then `invalidate_pane_snapshot` + `request_pane_capture` per pane (D9).
    - `cargo test --manifest-path desktop-gpui/Cargo.toml --workspace` fully green with zero new packages.
  </acceptance_criteria>
  <action>
    Implement `set_theme_preset` per the reference port shape — do not invent a second mutation path (single mutation point). Port the reference card-grid settings view over the existing route (do not rebuild routing). Replace hardcoded chrome colors with preset-helper reads in the four chrome surfaces only — leave terminal renderer internals to the existing seams. Append the three icons (do not touch existing consts). Apply font live + viewport re-arm and scrollback recreate + re-capture per D9 (never recreate without re-capture — Pitfall 3; never font-apply without viewport re-arm — Pitfall 4). Verify with the full workspace battery as the gate.
  </action>
  <verify>
    <automated>cargo test --manifest-path desktop-gpui/Cargo.toml --workspace</automated>
    <fails_when>non-zero exit code or "test result: FAILED" in summary</fails_when>
  </verify>
  <done>
    Tracer slice complete: pick a theme card → all chrome + terminals re-theme live → prefs persist → restart restores preset + geometry; terminal prefs apply live; full workspace suite green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FE data files → generator → checked-in Rust | Build-time data crosses here; output is reviewed diff, lengths test pins counts |
| Settings file → DesktopSettings | Untrusted persisted JSON crosses here (legacy + corrupt inputs) |
| Pref inputs (font/scrollback numbers) → terminal engine | User numerics flow into renderer construction + tmux geometry |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Tampering | `themes_generated.rs` (generator output) | medium | mitigate | Output checked in as reviewable diff; lengths/link/spot-hex tests pin 102/78 + resolution + `default-dark.background == 0x1e1e1e`; regen documented in script header |
| T-06-02 | Tampering | `DesktopSettings` new fields | low | mitigate | Additive serde defaults only — legacy/corrupt files load with safe defaults; existing `.bak` recovery untouched |
| T-06-03 | Denial of service | Terminal pref numeric inputs | low | mitigate | FE-verbatim clamps (font 8–32, line-height 1–2, scrollback 100–50000) enforced in pure helpers before any engine apply |
| T-06-SC | Tampering | Cargo dependencies | high | mitigate | Zero new packages; milestone-pinned workspace with committed `Cargo.lock` |
</threat_model>

<verification>
Run the single-filter theme gate then the full workspace battery (reuse
`CARGO_TARGET_DIR=C:\cargo-target\web-tmux` with `CARGO_BUILD_JOBS=2..4`):
```bash
cargo test -p webtmux theme_tables
cargo test --manifest-path desktop-gpui/Cargo.toml --workspace
```
Phase gate: full suite green + manual UAT class items (card-grid pixels, live re-theme flash,
restart restore, font render) recorded in 06-VALIDATION.md for the Phase-7 parity audit.
</verification>

<success_criteria>
1. 102 UI + 78 terminal presets checked in with lengths/link/luminance/filter tests green (THEME-01/THEME-03).
2. Theme pick re-themes all chrome live with linked terminal palettes; preset + geometry persist and restore (THEME-02/SHELL-04).
3. Settings page card grid + filter + terminal prefs render and apply live with FE clamps (SET-01/SET-02).
4. Embedded JetBrains Mono + 3 new lucide icons; no system font/icon deps (SHELL-05).
5. Full workspace suite green with zero new packages.
</success_criteria>

<output>
Create `.planning/phases/06-theme-settings-chrome-parity/06-01-SUMMARY.md` when done
</output>
