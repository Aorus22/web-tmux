---
phase: 05-pane-grid-workspace-operations
plan: "02"
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - desktop-gpui/crates/webtmux/src/app_state.rs
  - desktop-gpui/crates/webtmux/src/views/mod.rs
  - desktop-gpui/crates/webtmux/src/views/pane_grid.rs
  - desktop-gpui/crates/webtmux/src/views/pane_context_menu.rs
  - desktop-gpui/crates/webtmux/src/views/rename_pane_dialog.rs
  - desktop-gpui/crates/webtmux/src/views/rename_window_dialog.rs
  - desktop-gpui/crates/webtmux/src/views/window_toolbar.rs
  - desktop-gpui/crates/webtmux/src/views/tab_strip.rs
  - desktop-gpui/crates/webtmux/tests/pane_ops_test.rs
autonomous: true
requirements:
  - PANE-04
  - PANE-05
  - PANE-06
  - PANE-08
  - DLG-02

estimate:
  tokens: 60000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Dragging pane dividers resizes via throttled incremental cell steps (~40ms) including negative-direction flips, without resize storms (criterion 3, PANE-04 per D3)"
    - "Pane context menu kills/renames/swaps/breaks panes (join excluded — D8 backend gap) and the title-bar tab context menu renames/moves/breaks/kills windows (criterion 4, PANE-05/PANE-08 per D7/D8)"
    - "Every rename flow (session done in Phase 3; pane + window here) opens a dialog with a real text input (criterion 4, DLG-02 per D6)"
    - "Layout presets (even-h, even-v, main-h, main-v, tiled, next) apply from the window toolbar (criterion 5 preset half, PANE-06 per D7)"
    - "Title-bar tabs gain the Plus button (window.create, no args) plus chip context menus that Phase 3 carved out (PANE-08 per D8)"
    - "Swap picker lists same-window panes only and disables when empty; kill entries honor confirm_kill_pane/window gates (PANE-05 per D7)"
    - { statement: "Fast drags past the 4px handle keep streaming moves and menus survive poll re-renders", verification: "backstop" }
  prohibitions:
    - "No pane.join invented (no WS route, no FE surface — D8 descopes it to a logged backend gap)"
    - "No cumulative drag displacement sent (always subtract last_cells — FE geometry.ts:133-146 documents the overshoot)"
    - "No per-step drag awaits (receiver dropped; pending map self-cleans)"
    - "No unstable menu element ids (no indices/counters — poll re-render drops open menus otherwise)"
  artifacts:
    - path: "desktop-gpui/crates/webtmux/src/views/pane_context_menu.rs"
      provides: "9-row pane menu + same-window swap picker + kill-confirm routing"
    - path: "desktop-gpui/crates/webtmux/src/views/rename_pane_dialog.rs"
      provides: "DLG1-clone Rename Pane dialog (prefill title||current_command, Enter-submit)"
    - path: "desktop-gpui/crates/webtmux/src/views/rename_window_dialog.rs"
      provides: "DLG1-clone Rename Window dialog (prefill name, Enter-submit)"
    - path: "desktop-gpui/crates/webtmux/src/views/window_toolbar.rs"
      provides: "h-8 preset bar (5 presets + Next) sending window.layout"
    - path: "desktop-gpui/crates/webtmux/src/views/tab_strip.rs"
      provides: "Plus button + chip context menus on title-bar tabs (Phase-3 D8 carve-out)"
  key_links:
    - from: "desktop-gpui/crates/webtmux/src/views/pane_grid.rs"
      to: "desktop-gpui/crates/webtmux/src/pane_geometry.rs"
      via: "Divider handles stream mouse moves through resize_drag_step into fire-and-forget pane.resize"
      pattern: "resize_drag_step"
    - from: "desktop-gpui/crates/webtmux/src/views/pane_context_menu.rs"
      to: "desktop-gpui/crates/webtmux/src/app_state.rs"
      via: "Menu actions invoke AppState correlated pane sends; kill branches on confirm gates"
      pattern: "submit_pane"
    - from: "desktop-gpui/crates/webtmux/src/views/tab_strip.rs"
      to: "desktop-gpui/crates/webtmux/src/app_state.rs"
      via: "Plus and chip-menu actions invoke AppState correlated window sends with paneId-carried @N"
      pattern: "submit_window"
---

<objective>
Complete Phase 5 on top of the 05-01 tracer grid: divider drags with 40ms throttle + FLIP (PANE-04), pane context menu + rename dialog (PANE-05 + DLG-02), window toolbar presets (PANE-06), and title-bar Plus + chip menus + window rename dialog (PANE-08 + DLG-02).

Purpose: Deliver every remaining workspace operation — drags, menus, presets, window-tab ops — reusing the 05-01 sends, gates, prefill helpers, and icons.
Output: Drag state machine, pane/window menus + dialogs, toolbar, extended tab strip, and pane-ops headless tests.
</objective>

<execution_context>
@C:/Users/alyza/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/alyza/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-pane-grid-workspace-operations/05-CONTEXT.md
@.planning/phases/05-pane-grid-workspace-operations/05-RESEARCH.md
@.planning/phases/05-pane-grid-workspace-operations/05-UI-SPEC.md
@.planning/phases/05-pane-grid-workspace-operations/05-VALIDATION.md
@.planning/phases/05-pane-grid-workspace-operations/05-01-PLAN.md
@desktop-gpui/crates/webtmux/src/app_state.rs
@desktop-gpui/crates/webtmux/src/views/pane_grid.rs
@desktop-gpui/crates/webtmux/src/views/tab_strip.rs
@desktop-gpui/crates/webtmux/src/views/session_context_menu.rs
</context>

<assumption_delta_decision>
no-change: Drag throttle/FLIP, menu item lists, preset strings, and dialog shapes are verbatim copies of the Read-verified FE sources via the 05-01-tested helpers.
</assumption_delta_decision>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement divider drag state machine with 40ms throttle + FLIP</name>
  <files>
    desktop-gpui/crates/webtmux/src/app_state.rs,
    desktop-gpui/crates/webtmux/src/views/pane_grid.rs,
    desktop-gpui/crates/webtmux/tests/pane_ops_test.rs
  </files>
  <read_first>
    E:/Coding Stuff/web-tmux/.planning/phases/05-pane-grid-workspace-operations/05-RESEARCH.md,
    E:/Coding Stuff/web-tmux/fe/src/features/panes/PaneResizeHandle.tsx,
    E:/Coding Stuff/web-tmux/fe/src/features/panes/PaneWorkspace.tsx,
    E:/Coding Stuff/web-tmux/fe/src/lib/geometry.ts,
    E:/Coding Stuff/web-tmux/desktop-gpui/crates/webtmux/src/views/terminal_view.rs,
    E:/Coding Stuff/web-tmux/desktop-gpui/crates/webtmux/src/views/pane_grid.rs
  </read_first>
  <behavior>
    - Unit test `test_drag_throttle`: first move inside the 40ms window sends nothing and does NOT advance `last_cells`; the move after the window sends the full accumulated step (PANE-04 per D3).
    - Unit test `test_drag_flip`: negative displacement returns the FLIP direction with positive amount; zero steps drop (PANE-04 per D3).
    - Unit test `test_drag_no_viewport_send`: drag-path helper constructs only `pane.resize` envelopes — never `terminal.resize`, never `hello` (PANE-04 pitfall lock).
  </behavior>
  <acceptance_criteria>
    - `AppState` (or a pure drag helper it owns) holds per-drag `(pane_id, direction, start_px, last_cells, last_sent: Instant)`; `mouse_down` on a divider handle records it, `mouse_move` (copied from the `terminal_view.rs:345-379` pattern) computes `px_to_cells(pos - start, cell) - last_cells`, applies the step==0 / 40ms-throttle / FLIP rules from `PaneResizeHandle.tsx:30-57,80-94`, and fires `submit_pane_resize` (receiver dropped).
    - Handles use `cellPx` from the measured cell size; cursor col-resize (vertical) / row-resize (horizontal).
    - The Phase-4 layout-key timer shape keeps self-debouncing mid-drag (timers reset on change, fire 150/325ms after the LAST step only) — no drag storms.
  </acceptance_criteria>
  <action>
    Build the drag state machine per D3 on top of the 05-01 `resize_drag_step` helper (already unit-locked — this task wires timing + sends). Reuse the terminal_view mouse-move streaming pattern; if moves stop outside the 4px handle on fast drags, add pointer capture and note it in the SUMMARY (research A2). Keep the paint path arm-only with zero-size early return.
  </action>
  <verify>
    <automated>cargo test -p webtmux pane_ops</automated>
    <fails_when>non-zero exit code or "test result: FAILED" in summary</fails_when>
  </verify>
  <done>
    Divider drags resize via throttled incremental steps with FLIP, no storms, no viewport sends, and drag tests pass green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement pane context menu + rename dialog + window toolbar presets</name>
  <files>
    desktop-gpui/crates/webtmux/src/views/mod.rs,
    desktop-gpui/crates/webtmux/src/views/pane_context_menu.rs,
    desktop-gpui/crates/webtmux/src/views/rename_pane_dialog.rs,
    desktop-gpui/crates/webtmux/src/views/window_toolbar.rs,
    desktop-gpui/crates/webtmux/src/views/pane_view.rs,
    desktop-gpui/crates/webtmux/src/app_state.rs
  </files>
  <read_first>
    E:/Coding Stuff/web-tmux/.planning/phases/05-pane-grid-workspace-operations/05-RESEARCH.md,
    E:/Coding Stuff/web-tmux/.planning/phases/05-pane-grid-workspace-operations/05-UI-SPEC.md,
    E:/Coding Stuff/web-tmux/fe/src/features/panes/PaneContextMenu.tsx,
    E:/Coding Stuff/web-tmux/fe/src/features/windows/WindowToolbar.tsx,
    E:/Coding Stuff/web-tmux/fe/src/features/windows/LayoutSelector.tsx,
    E:/Coding Stuff/web-tmux/desktop-gpui/crates/webtmux/src/views/rename_session_dialog.rs,
    E:/Coding Stuff/web-tmux/desktop-gpui/crates/webtmux/src/views/session_context_menu.rs
  </read_first>
  <behavior>
    - Unit test `test_swap_picker_scope`: picker helper filters `snapshot.panes` to `windowId == activeWindowId`, excludes the current pane, and reports disabled when empty (PANE-05 pitfall lock).
    - Unit test `test_layout_strings`: preset helper maps the six toolbar buttons to `even-horizontal`, `even-vertical`, `main-horizontal`, `main-vertical`, `tiled`, `next-layout` verbatim (PANE-06 per D7).
  </behavior>
  <acceptance_criteria>
    - `pane_context_menu.rs` renders the 9-row FE menu order verbatim (incl. separators) with stable `pane-menu/%N` ids: Split right / Split down / Zoom / Rename / Swap (picker submenu `currentCommand || title || currentPath`, disabled when no same-window others) / Break pane / separator / Kill (destructive, gated by `confirm_kill_pane`). Join is ABSENT with a code comment citing the D8 backend gap.
    - `rename_pane_dialog.rs` clones DLG1 per D6: Title "Rename Pane", Label "New name", prefill `title || current_command || ''`, autofocus, Enter submits, Rename disabled when empty/busy, inline error; sends `pane.rename { paneId: %N, title }` via the 05-01 correlated path.
    - `window_toolbar.rs` renders the h-8 bar with 5 preset icon buttons + separator + Next, sending `window.layout { paneId: @N (active window), layout }` correlated; `command.error` surfaces via the inline-error convention.
    - Pane rows/menus wire kill through the 05-01 pane gate (confirm dialog vs direct kill).
  </acceptance_criteria>
  <action>
    Clone `rename_session_dialog.rs` + `session_context_menu.rs` shapes per D6/D7, copying the FE item lists verbatim — do not reorder, rename, or "improve" menu rows. Filter swap candidates by active window per the research pitfall. Mount the toolbar above the grid for the active window. Register all new modules in `views/mod.rs`.
  </action>
  <verify>
    <automated>cargo test -p webtmux pane_ops</automated>
    <fails_when>non-zero exit code or "test result: FAILED" in summary</fails_when>
  </verify>
  <done>
    Pane menu (minus join), rename dialog, and preset toolbar work through correlated sends with headless scope/string tests green.
  </done>
</task>

<task type="auto">
  <name>Task 3: Extend title-bar tabs with Plus button + chip menus + window rename dialog</name>
  <files>
    desktop-gpui/crates/webtmux/src/views/mod.rs,
    desktop-gpui/crates/webtmux/src/views/tab_strip.rs,
    desktop-gpui/crates/webtmux/src/views/rename_window_dialog.rs,
    desktop-gpui/crates/webtmux/src/app_state.rs
  </files>
  <read_first>
    E:/Coding Stuff/web-tmux/.planning/phases/05-pane-grid-workspace-operations/05-RESEARCH.md,
    E:/Coding Stuff/web-tmux/.planning/phases/05-pane-grid-workspace-operations/05-UI-SPEC.md,
    E:/Coding Stuff/web-tmux/fe/src/features/windows/WindowTabs.tsx,
    E:/Coding Stuff/web-tmux/fe/src/features/windows/WindowContextMenu.tsx,
    E:/Coding Stuff/web-tmux/desktop-gpui/crates/webtmux/src/views/tab_strip.rs,
    E:/Coding Stuff/web-tmux/desktop-gpui/crates/webtmux/src/views/rename_session_dialog.rs
  </read_first>
  <acceptance_criteria>
    - `tab_strip.rs` gains the Plus button (sends `window.create` with no args, correlated) and per-chip context menus with stable `window-tab/@N` ids: Rename / Move Left (`window.move` offset -1) / Move Right (+1) / Break active (`window.break-active`) / separator / Kill (destructive, gated by `confirm_kill_window`). Chip click keeps the 03-02 fire-and-forget `window.select`.
    - `rename_window_dialog.rs` clones DLG1 per D6: Title "Rename Window", Label "New name", prefill `w.name`, autofocus, Enter submits, Rename disabled when empty/busy, inline error; sends `window.rename { paneId: @N, name }`.
    - All window targets ride `pane_id: Some("@N")` (no `windowId` field). Full workspace suite green.
  </acceptance_criteria>
  <action>
    Extend `tab_strip.rs` in place per D8 (do not rebuild the tabs — keep the 03-02 chips, divider, Settings gear). Clone the DLG1 dialog shape for the window rename. Route Plus + menu actions through the 05-01 `submit_window_*` sends. Verify with the full workspace battery as the gate.
  </action>
  <verify>
    <automated>cargo test --manifest-path desktop-gpui/Cargo.toml --workspace</automated>
    <fails_when>non-zero exit code or "test result: FAILED" in summary</fails_when>
  </verify>
  <done>
    Title-bar tabs support create + chip menus + rename + move + break + gated kill; Phase 5 all five success criteria hold end-to-end (join excluded per D8).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App → Go Backend WS (typed commands) | Drag steps + menu/toolbar/tab actions ride correlated sends; rename titles cross as JSON fields |
| Pointer input → drag state machine | Untrusted pixel deltas flow into cell-step math and command amounts |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-04 | Tampering | `rename_window_dialog` name input | medium | mitigate | Name travels as JSON `name` field into argv (no shell); trim + non-empty gate; typed dispatch only |
| T-05-05 | Denial of service | Drag-command flood | medium | mitigate | 40ms throttle + `step==0` drop + dropped receivers (pending map self-cleans via forget-timeout); layout-key timers self-debounce so no capture storms |
| T-05-06 | Denial of service | Swap/move targeting | low | mitigate | Same-window filter on swap candidates; move offsets exactly ±1; window targets via `paneId @N` — server rejects anything unknown |
| T-05-SC | Tampering | Cargo dependencies | high | mitigate | Zero new packages; milestone-pinned workspace with committed `Cargo.lock` |
</threat_model>

<verification>
Run the pane-ops suite then the full workspace battery (single-filter rule — one TESTNAME per invocation; reuse `CARGO_TARGET_DIR=C:\cargo-target\web-tmux` with `CARGO_BUILD_JOBS=4`):
```bash
cargo test -p webtmux pane_ops
cargo test --manifest-path desktop-gpui/Cargo.toml --workspace
```
Phase gate: full suite green + manual UAT class items (pixel parity vs Electron, dialog typing, drag feel incl. fast-drag capture, menu poll-tick survival) recorded in 05-VALIDATION.md for the Phase-7 parity audit.
</verification>

<success_criteria>
1. Divider drags throttle at ~40ms with FLIP and no storms; drag path never emits `terminal.resize` or `hello`.
2. Pane menu (kill/rename/swap/break, no join) + rename dialog + toolbar presets all work via correlated sends.
3. Title-bar Plus + chip menus + window rename/move/break/kill complete PANE-08 and DLG-02.
4. Full workspace suite green with zero new packages; join logged as backend gap.
</success_criteria>

<output>
Create `.planning/phases/05-pane-grid-workspace-operations/05-02-PLAN.md` when done
</output>
